# 📱 小程序视频面试完整解决方案

## 🎯 目标

在微信小程序中实现**完整的视频面试功能**，包括：
- HR创建面试房间
- HR以主持人身份进入
- 分享给访客（求职者）
- 访客加入面试
- 完整的视频通话功能

---

## 🏗️ 架构设计

### 方案选择：混合架构（推荐）

```
小程序端                          H5端（WebView）
┌─────────────────┐              ┌──────────────────┐
│                 │              │                  │
│  1. 创建房间页  │              │  视频通话界面    │
│  - 生成房间号   │              │  - ZEGO SDK      │
│  - 输入信息     │              │  - 音视频控制    │
│  - 开始面试     │──跳转到──→  │  - 参与者管理    │
│                 │              │  - 屏幕共享      │
│  2. 访客加入页  │              │  - 美颜功能      │
│  - 扫码/分享    │              │                  │
│  - 填写信息     │──跳转到──→  │                  │
│  - 加入面试     │              │                  │
│                 │              │                  │
│  3. 房间列表页  │              │                  │
│  - 历史记录     │              │                  │
│  - 快速进入     │              │                  │
└─────────────────┘              └──────────────────┘
```

**为什么选择混合架构？**
1. ✅ 小程序原生UI体验更好（创建、加入流程）
2. ✅ WebView承载复杂的视频SDK（避免小程序包体积限制）
3. ✅ 复用现有H5视频功能（开发效率高）
4. ✅ 避免小程序审核风险（视频SDK在WebView中）

---

## 📁 小程序页面结构

```
miniprogram/
├── pages/
│   ├── index/                    # 首页
│   │   ├── index.wxml
│   │   ├── index.js
│   │   ├── index.json
│   │   └── index.wxss
│   │
│   ├── interview/
│   │   ├── create/               # 创建面试房间
│   │   │   ├── create.wxml
│   │   │   ├── create.js
│   │   │   ├── create.json
│   │   │   └── create.wxss
│   │   │
│   │   ├── host/                 # HR主持人视频页面
│   │   │   ├── host.wxml         # WebView加载主持人H5
│   │   │   ├── host.js
│   │   │   ├── host.json
│   │   │   └── host.wxss
│   │   │
│   │   ├── guest/                # 访客加入页面
│   │   │   ├── guest.wxml        # 填写信息表单
│   │   │   ├── guest.js
│   │   │   ├── guest.json
│   │   │   └── guest.wxss
│   │   │
│   │   ├── video/                # 访客视频页面
│   │   │   ├── video.wxml        # WebView加载访客H5
│   │   │   ├── video.js
│   │   │   ├── video.json
│   │   │   └── video.wxss
│   │   │
│   │   └── list/                 # 房间列表（可选）
│   │       ├── list.wxml
│   │       ├── list.js
│   │       ├── list.json
│   │       └── list.wxss
│   │
│   └── login/                    # 登录页（如需要）
│       ├── login.wxml
│       ├── login.js
│       ├── login.json
│       └── login.wxss
│
├── utils/
│   ├── api.js                    # API请求封装
│   ├── auth.js                   # 登录认证
│   └── util.js                   # 工具函数
│
├── app.js                        # 小程序入口
├── app.json                      # 全局配置
└── app.wxss                      # 全局样式
```

---

## 🔄 完整流程

### 流程1：HR创建面试

```
HR打开小程序
    ↓
进入"创建面试"页面
    ↓
点击"生成房间号"（自动生成）
    ↓
输入HR姓名（可选，默认用登录用户）
    ↓
点击"开始面试"
    ↓
跳转到 host 页面（WebView加载主持人H5）
    ↓
以主持人身份进入视频房间
    ↓
点击"邀请他人"
    ↓
生成小程序分享卡片/小程序码
    ↓
分享给求职者
```

### 流程2：访客加入面试

```
求职者收到分享
    ↓
点击小程序卡片/扫小程序码
    ↓
进入"访客加入"页面（guest）
    ↓
填写姓名和身份
    ↓
点击"加入面试"
    ↓
跳转到 video 页面（WebView加载访客H5）
    ↓
以访客身份进入视频房间
    ↓
开始面试
```

---

## 📄 页面详细设计

### 1. 创建面试页面 (pages/interview/create/)

**功能**：
- 生成房间号
- 输入HR信息
- 开始面试

**UI设计**：
```
┌─────────────────────────────┐
│      📹 创建视频面试         │
├─────────────────────────────┤
│                             │
│  房间号                     │
│  ┌─────────────────────┐   │
│  │ room_1234567_abc    │   │
│  └─────────────────────┘   │
│  [🔄 重新生成]              │
│                             │
│  您的姓名                   │
│  ┌─────────────────────┐   │
│  │ 张经理              │   │
│  └─────────────────────┘   │
│                             │
│  ┌─────────────────────┐   │
│  │   🎥 开始面试       │   │
│  └─────────────────────┘   │
│                             │
│  ┌─────────────────────┐   │
│  │   📋 历史记录       │   │
│  └─────────────────────┘   │
│                             │
└─────────────────────────────┘
```

### 2. HR主持人视频页面 (pages/interview/host/)

**功能**：
- WebView加载 `/interview/video-mobile/:roomId`
- 以主持人身份进入
- 完整的主持人权限

**实现**：
```xml
<!-- host.wxml -->
<web-view 
  src="{{webviewUrl}}" 
  bindmessage="handleMessage"
></web-view>
```

```javascript
// host.js
Page({
  data: {
    webviewUrl: '',
    roomId: ''
  },
  
  onLoad(options) {
    const { roomId, userName } = options;
    const url = `https://crm.andejiazheng.com/interview/video-mobile/${roomId}?userName=${encodeURIComponent(userName)}`;
    this.setData({ webviewUrl: url, roomId });
  },
  
  // 分享给访客
  onShareAppMessage() {
    return {
      title: '视频面试邀请',
      path: `/pages/interview/guest/guest?roomId=${this.data.roomId}`,
      imageUrl: '/images/interview-share.png'
    };
  }
});
```

### 3. 访客加入页面 (pages/interview/guest/)

**功能**：
- 显示房间信息
- 填写姓名和身份
- 加入面试

**UI设计**：
```
┌─────────────────────────────┐
│      加入视频面试           │
├─────────────────────────────┤
│                             │
│  房间号：room_1234567_abc   │
│                             │
│  * 您的姓名                 │
│  ┌─────────────────────┐   │
│  │ 请输入您的姓名      │   │
│  └─────────────────────┘   │
│                             │
│  * 您的身份                 │
│  ┌─────────────────────┐   │
│  │ 请选择您的身份  ▼  │   │
│  └─────────────────────┘   │
│  - 求职者                   │
│  - 家政人员                 │
│  - 其他                     │
│                             │
│  ┌─────────────────────┐   │
│  │   ✅ 加入面试       │   │
│  └─────────────────────┘   │
│                             │
│  💡 温馨提示：              │
│  • 请确保网络连接稳定       │
│  • 请允许访问摄像头和麦克风 │
│                             │
└─────────────────────────────┘
```

### 4. 访客视频页面 (pages/interview/video/)

**功能**：
- WebView加载 `/interview/join-mobile/:roomId`
- 以访客身份进入
- 受限的访客权限

**实现**：
```xml
<!-- video.wxml -->
<web-view 
  src="{{webviewUrl}}" 
  bindmessage="handleMessage"
></web-view>
```

```javascript
// video.js
Page({
  data: {
    webviewUrl: '',
    roomId: ''
  },
  
  onLoad(options) {
    const { roomId, userName, identity } = options;
    const url = `https://crm.andejiazheng.com/interview/join-mobile/${roomId}?userName=${encodeURIComponent(userName)}&identity=${identity}`;
    this.setData({ webviewUrl: url, roomId });
  },
  
  handleMessage(e) {
    const messages = e.detail.data;
    if (messages && messages.length > 0) {
      const lastMessage = messages[messages.length - 1];
      if (lastMessage.type === 'leave') {
        wx.showModal({
          title: '提示',
          content: '面试已结束',
          showCancel: false,
          success: () => {
            wx.navigateBack();
          }
        });
      }
    }
  }
});
```

---

## 🔗 URL参数传递

### HR主持人
```
小程序页面：pages/interview/create/create
    ↓ 点击"开始面试"
小程序页面：pages/interview/host/host?roomId=xxx&userName=xxx
    ↓ WebView加载
H5页面：https://crm.andejiazheng.com/interview/video-mobile/xxx?userName=xxx
```

### 访客
```
小程序页面：pages/interview/guest/guest?roomId=xxx
    ↓ 填写信息，点击"加入面试"
小程序页面：pages/interview/video/video?roomId=xxx&userName=xxx&identity=xxx
    ↓ WebView加载
H5页面：https://crm.andejiazheng.com/interview/join-mobile/xxx?userName=xxx&identity=xxx
```

---

## 📤 分享功能实现

### 方式1：小程序分享卡片

在 `host.js` 中实现：

```javascript
onShareAppMessage() {
  return {
    title: '视频面试邀请',
    path: `/pages/interview/guest/guest?roomId=${this.data.roomId}`,
    imageUrl: '/images/interview-share.png' // 需要准备分享图片
  };
}
```

### 方式2：小程序码（推荐）

在创建房间后，调用后端API生成小程序码：

```javascript
// 在 create.js 中
async generateQRCode() {
  const { roomId } = this.data;
  
  // 调用后端API生成小程序码
  const res = await wx.request({
    url: 'https://crm.andejiazheng.com/api/wechat/generate-qrcode',
    method: 'POST',
    data: {
      page: 'pages/interview/guest/guest',
      scene: `roomId=${roomId}`
    }
  });
  
  // 显示小程序码
  this.setData({
    qrcodeUrl: res.data.qrcodeUrl
  });
}
```

---

## 🔐 登录认证

### 方案1：微信登录（推荐）

```javascript
// utils/auth.js
async function wxLogin() {
  // 1. 获取微信登录code
  const { code } = await wx.login();
  
  // 2. 发送到后端换取token
  const res = await wx.request({
    url: 'https://crm.andejiazheng.com/api/wechat/login',
    method: 'POST',
    data: { code }
  });
  
  // 3. 保存token
  wx.setStorageSync('token', res.data.token);
  wx.setStorageSync('userInfo', res.data.userInfo);
  
  return res.data;
}
```

### 方案2：手机号登录

```javascript
// 使用微信手机号快速验证
<button open-type="getPhoneNumber" bindgetphonenumber="getPhoneNumber">
  手机号登录
</button>
```

---

## 🎨 UI组件库推荐

### 推荐使用 Vant Weapp

```bash
# 安装
npm install @vant/weapp -S --production

# 构建
npm run build
```

**优势**：
- 组件丰富
- 样式美观
- 文档完善
- 维护活跃

**使用示例**：
```xml
<!-- 使用Vant的表单组件 -->
<van-cell-group>
  <van-field
    label="姓名"
    placeholder="请输入您的姓名"
    bind:change="onNameChange"
  />
  <van-field
    label="身份"
    placeholder="请选择身份"
    is-link
    readonly
    bind:click="showIdentityPicker"
  />
</van-cell-group>

<van-button type="primary" block bind:click="joinInterview">
  加入面试
</van-button>
```

---

## 📦 需要后端支持的API

### 1. 生成房间号
```
POST /api/interview/create-room
Response: { roomId: "room_xxx", roomName: "xxx" }
```

### 2. 获取房间信息
```
GET /api/interview/room/:roomId
Response: { roomId, roomName, status, participants }
```

### 3. 生成小程序码
```
POST /api/wechat/generate-qrcode
Body: { page, scene }
Response: { qrcodeUrl }
```

### 4. 微信登录
```
POST /api/wechat/login
Body: { code }
Response: { token, userInfo }
```

---

## ⚙️ app.json 配置

```json
{
  "pages": [
    "pages/index/index",
    "pages/interview/create/create",
    "pages/interview/host/host",
    "pages/interview/guest/guest",
    "pages/interview/video/video"
  ],
  "window": {
    "backgroundTextStyle": "light",
    "navigationBarBackgroundColor": "#5DBFB3",
    "navigationBarTitleText": "视频面试",
    "navigationBarTextStyle": "white"
  },
  "permission": {
    "scope.camera": {
      "desc": "用于视频面试"
    },
    "scope.record": {
      "desc": "用于视频面试"
    }
  },
  "requiredPrivateInfos": [
    "getLocation",
    "chooseAddress"
  ]
}
```

---

## 🚀 开发步骤

### 第1步：创建页面结构（30分钟）
1. 创建所有页面目录和文件
2. 配置 app.json
3. 测试页面跳转

### 第2步：实现创建房间页面（1小时）
1. UI布局
2. 生成房间号逻辑
3. 跳转到主持人页面

### 第3步：实现主持人页面（30分钟）
1. WebView加载H5
2. 实现分享功能
3. 处理消息通信

### 第4步：实现访客加入页面（1小时）
1. UI布局
2. 表单验证
3. 跳转到访客视频页面

### 第5步：实现访客视频页面（30分钟）
1. WebView加载H5
2. 处理消息通信
3. 返回逻辑

### 第6步：测试和优化（1小时）
1. 完整流程测试
2. 异常处理
3. UI优化

**总计：约4-5小时**

---

## 📝 下一步

我将为您生成：
1. ✅ 每个页面的完整代码
2. ✅ API接口文档
3. ✅ 测试用例
4. ✅ 部署指南

需要我现在开始生成这些文件吗？

