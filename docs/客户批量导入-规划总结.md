# 客户批量导入功能 - 规划总结

## 📋 规划概述

本文档是客户批量导入功能的规划总结，整合了所有相关文档和设计决策。

**功能名称**: 客户批量导入  
**功能描述**: 支持通过Excel文件批量导入客户数据  
**必填字段**: 姓名、电话、线索来源  
**规划时间**: 2025-01-11  
**预计工时**: 10小时

---

## 🎯 核心需求

### 业务需求

1. **批量导入**: 支持一次性导入多条客户记录
2. **数据验证**: 确保导入数据的完整性和正确性
3. **去重处理**: 防止手机号重复
4. **错误反馈**: 详细列出导入失败的记录和原因
5. **模板下载**: 提供标准的Excel模板

### 技术需求

1. **文件格式**: 支持 .xlsx 和 .xls 格式
2. **文件大小**: 限制最大10MB
3. **并发处理**: 使用异步批量创建提高性能
4. **临时文件**: 自动清理上传的临时文件
5. **权限控制**: 需要登录和相应角色权限

---

## 📊 数据结构

### Excel表头（必填）

| 列名 | 说明 | 示例 |
|------|------|------|
| 姓名 | 客户姓名 | 张三 |
| 电话 | 客户手机号（11位） | 13800138000 |
| 线索来源 | 必须在枚举值内 | 美团 |

### Excel表头（可选）

| 列名 | 说明 | 示例 |
|------|------|------|
| 微信号 | 客户微信号 | wx123 |
| 身份证号 | 客户身份证号 | 110101199001011234 |
| 需求品类 | 服务类型 | 月嫂 |
| 客户状态 | 签约状态 | 待定 |
| 线索等级 | A/B/C/D类 | A类 |
| 薪资预算 | 数字，1000-50000 | 8000 |
| 期望上户日期 | 日期格式 | 2025-02-01 |
| 家庭面积 | 数字，10-1000 | 120 |
| 家庭人口 | 数字，1-20 | 4 |
| 休息制度 | 单休/双休等 | 单休 |
| 地址 | 详细地址 | 北京市朝阳区 |
| 备注 | 其他信息 | 备注信息 |

---

## 🏗️ 技术架构

### 后端架构

```
┌─────────────────────────────────────────┐
│         Controller Layer                │
│  POST /api/customers/import-excel       │
│  - FileInterceptor                      │
│  - 文件验证                              │
│  - 权限控制                              │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│         Service Layer                   │
│  importFromExcel()                      │
│  - ExcelJS 解析                         │
│  - 表头验证                              │
│  - 数据映射                              │
│  - 批量创建                              │
│  - 结果统计                              │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│         Database Layer                  │
│  MongoDB - customers 集合                │
│  - 手机号唯一索引                         │
│  - 数据验证                              │
└─────────────────────────────────────────┘
```

### 前端架构

```
┌─────────────────────────────────────────┐
│         CustomerList.tsx                │
│  - 导入按钮                              │
│  - 导入弹窗                              │
│  - 文件上传                              │
│  - 模板下载                              │
│  - 结果展示                              │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│         API Service                     │
│  apiService.upload()                    │
│  - FormData 封装                        │
│  - 错误处理                              │
└─────────────────────────────────────────┘
```

---

## 🔄 数据流程

### 导入流程

```
用户操作
  ↓
点击"批量导入"
  ↓
显示导入弹窗
  ↓
下载模板（可选）
  ↓
选择/拖拽Excel文件
  ↓
前端验证文件类型
  ↓
上传到后端
  ↓
后端接收文件
  ↓
保存到临时目录
  ↓
ExcelJS解析文件
  ↓
验证表头
  ↓
逐行解析数据
  ↓
数据验证和映射
  ↓
批量创建客户
  ↓
统计结果
  ↓
清理临时文件
  ↓
返回结果
  ↓
前端展示结果
  ↓
刷新客户列表
```

### 错误处理流程

```
数据验证失败
  ↓
记录错误信息
  ↓
继续处理下一行
  ↓
所有行处理完成
  ↓
返回错误详情
  ↓
前端展示错误列表
```

---

## 📁 文件清单

### 规划文档

1. **客户批量导入功能规划.md** - 完整的功能规划文档
   - 需求分析
   - 技术架构
   - 实现方案
   - 代码示例
   - 测试用例

2. **客户批量导入-快速开始.md** - 快速实现指南
   - 5分钟快速上手
   - 分步骤实现
   - 代码片段
   - 常见问题

3. **客户批量导入-实现检查清单.md** - 详细的实现清单
   - 37个检查项
   - 9个实现阶段
   - 进度跟踪
   - 验收标准

4. **客户批量导入-规划总结.md** - 本文档
   - 规划概述
   - 核心需求
   - 技术架构
   - 关键决策

### 可视化图表

1. **客户批量导入架构图** - Mermaid图表
   - 前端层
   - API层
   - 服务层
   - 数据层

2. **客户批量导入流程图** - 时序图
   - 用户交互
   - 前后端通信
   - 数据处理
   - 错误处理

3. **Excel字段映射关系图** - 映射图
   - Excel列到DTO字段
   - 数据类型转换
   - 验证规则

4. **枚举值映射关系图** - 枚举图
   - 线索来源
   - 需求品类
   - 客户状态
   - 其他枚举

---

## 🎨 设计决策

### 1. 为什么选择 ExcelJS？

**决策**: 使用 ExcelJS 而不是 xlsx 或其他库

**理由**:
- ✅ 已在项目中使用（简历导入）
- ✅ 功能强大，支持复杂格式
- ✅ 性能良好
- ✅ 维护活跃
- ✅ TypeScript 支持好

### 2. 为什么使用异步批量创建？

**决策**: 使用 `Promise.all` 并发创建而不是串行创建

**理由**:
- ✅ 性能更好（并发处理）
- ✅ 不会因单条失败而中断
- ✅ 可以收集所有错误
- ✅ 用户体验更好

### 3. 为什么不支持更新已有客户？

**决策**: 手机号重复时直接失败，不支持更新

**理由**:
- ✅ 避免误操作覆盖数据
- ✅ 保持数据一致性
- ✅ 简化实现逻辑
- ✅ 如需更新可单独编辑

### 4. 为什么客户状态默认为"待定"？

**决策**: 导入时如未指定状态，默认为"待定"

**理由**:
- ✅ 新导入的客户通常需要进一步确认
- ✅ 避免误标记为"已签约"等状态
- ✅ 符合业务流程
- ✅ 可后续手动修改

### 5. 为什么使用CSV格式的模板？

**决策**: 模板下载为CSV格式而不是真正的Excel

**理由**:
- ✅ 前端生成简单
- ✅ 无需额外库
- ✅ Excel可以直接打开CSV
- ✅ 文件更小
- ✅ 兼容性好

---

## 🔑 关键技术点

### 1. 文件上传配置

```typescript
@UseInterceptors(FileInterceptor('file', {
  storage: diskStorage({
    destination: './uploads/temp',
    filename: (req, file, callback) => {
      const uniqueSuffix = Date.now() + '-' + Math.round(Math.random() * 1e9);
      const extension = extname(file.originalname);
      callback(null, `customer-excel-${uniqueSuffix}${extension}`);
    },
  }),
  fileFilter: (req, file, callback) => {
    const ext = extname(file.originalname).toLowerCase();
    if (!['.xlsx', '.xls'].includes(ext)) {
      return callback(new BadRequestException('仅支持 .xlsx 或 .xls 格式的Excel文件'), false);
    }
    callback(null, true);
  },
}))
```

**关键点**:
- 使用唯一文件名避免冲突
- 文件类型验证在上传时进行
- 临时目录统一管理

### 2. Excel解析

```typescript
const workbook = new ExcelJS.Workbook();
await workbook.xlsx.readFile(filePath);
const worksheet = workbook.getWorksheet(1);
```

**关键点**:
- 只读取第一个工作表
- 异步读取避免阻塞
- 错误处理完善

### 3. 表头验证

```typescript
const requiredColumns = ['姓名', '电话', '线索来源'];
const missingColumns = requiredColumns.filter(col => !headers.includes(col));
if (missingColumns.length > 0) {
  throw new BadRequestException(`Excel文件缺少必需的列: ${missingColumns.join(', ')}`);
}
```

**关键点**:
- 明确必需列
- 友好的错误提示
- 早期验证避免浪费资源

### 4. 批量创建

```typescript
const promises = [];
for (let rowNumber = 2; rowNumber <= worksheet.rowCount; rowNumber++) {
  // ... 解析数据
  promises.push(
    this.create(customerData, userId)
      .then(() => result.success++)
      .catch(error => {
        result.fail++;
        result.errors.push(`第 ${rowNumber} 行导入失败: ${error.message}`);
      })
  );
}
await Promise.all(promises);
```

**关键点**:
- 并发创建提高性能
- 错误不中断流程
- 详细记录每行错误

### 5. 临时文件清理

```typescript
try {
  // ... 处理逻辑
  fs.unlinkSync(filePath);
} catch (error) {
  if (fs.existsSync(filePath)) {
    fs.unlinkSync(filePath);
  }
  throw error;
}
```

**关键点**:
- 成功和失败都要清理
- 使用 try-finally 确保执行
- 检查文件存在再删除

---

## 📈 性能考虑

### 预期性能指标

| 数据量 | 预期时间 | 内存使用 |
|--------|---------|---------|
| 10条 | < 2秒 | < 50MB |
| 100条 | < 10秒 | < 100MB |
| 500条 | < 30秒 | < 200MB |
| 1000条 | < 60秒 | < 300MB |

### 性能优化策略

1. **并发创建**: 使用 Promise.all 而不是串行
2. **流式读取**: ExcelJS 支持流式读取大文件
3. **批量插入**: 可考虑使用 MongoDB 的 insertMany
4. **索引优化**: 确保手机号字段有索引
5. **内存管理**: 及时清理临时文件

---

## 🔒 安全考虑

### 安全措施

1. **文件类型验证**: 仅允许 .xlsx 和 .xls
2. **文件大小限制**: 最大10MB
3. **权限控制**: 需要登录和角色权限
4. **数据验证**: 严格的DTO验证
5. **临时文件清理**: 防止磁盘占用
6. **SQL注入防护**: 使用ORM避免注入
7. **XSS防护**: 前端转义显示内容

---

## 🎓 学习参考

### 参考实现

本功能参考了简历批量导入的实现：
- `backend/src/modules/resume/resume.controller.ts` (247-304行)
- `backend/src/modules/resume/resume.service.ts` (1218-1414行)
- `frontend/src/pages/aunt/ResumeList.tsx` (415-502行)

### 技术文档

- [ExcelJS 官方文档](https://github.com/exceljs/exceljs)
- [Multer 文档](https://github.com/expressjs/multer)
- [Ant Design Upload 组件](https://ant.design/components/upload-cn/)
- [NestJS File Upload](https://docs.nestjs.com/techniques/file-upload)

---

## ✅ 下一步行动

### 立即开始

1. 阅读 **客户批量导入-快速开始.md**
2. 按照 **客户批量导入-实现检查清单.md** 逐项实现
3. 参考 **客户批量导入功能规划.md** 了解详细设计

### 实现顺序

1. **Day 1**: 后端实现（5小时）
   - Controller 接口
   - Service 逻辑
   - 单元测试

2. **Day 2**: 前端实现（3小时）
   - 导入按钮和弹窗
   - 文件上传
   - 结果展示

3. **Day 3**: 测试和文档（2小时）
   - 集成测试
   - 用户文档
   - 部署上线

---

## 📞 支持和反馈

如有问题或建议，请：
1. 查看文档中的常见问题
2. 检查实现清单中的验收标准
3. 参考简历导入的实现

---

**文档版本**: 1.0  
**创建时间**: 2025-01-11  
**维护人**: AI Assistant  
**状态**: ✅ 规划完成，可以开始实现

