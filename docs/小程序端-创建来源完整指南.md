# 小程序端 - 创建来源完整指南

**版本**: v1.0.0  
**更新日期**: 2025-10-17  
**状态**: ✅ 生产环境已部署

---

## 🎯 核心概念

### 什么是创建来源？

**创建来源** = 这条简历是谁创建的

```
┌─────────────────────────────────────────────────────┐
│                  创建来源分类                         │
├─────────────────────────────────────────────────────┤
│ 🔵 自助注册                                          │
│    ├─ 定义: 阿姨通过小程序自己创建                  │
│    ├─ 使用场景: 本接口                              │
│    ├─ 后端标记: userId = null                       │
│    └─ CRM显示: 蓝色标签"自助注册"                   │
│                                                     │
│ 🟠 员工创建                                          │
│    ├─ 定义: 销售/HR通过CRM系统创建                  │
│    ├─ 使用场景: 不涉及小程序                        │
│    ├─ 后端标记: userId = ObjectId                   │
│    └─ CRM显示: 橙色标签"员工创建"                   │
└─────────────────────────────────────────────────────┘
```

### 小程序端的职责

✅ **你需要做的**:
1. 调用自助注册接口
2. 提供基本信息（姓名、手机号、年龄、工种等）
3. 处理响应和错误

❌ **你不需要做的**:
1. 不要传递 `userId` 字段
2. 不要传递 `createdBy` 字段
3. 不要传递 `leadSource` 字段
4. 不要尝试手动设置创建来源

---

## 📝 请求示例

### ✅ 正确做法

```javascript
// 调用自助注册接口
wx.request({
  url: 'https://crm.andejiazheng.com/api/resumes/miniprogram/self-register',
  method: 'POST',
  header: {
    'Content-Type': 'application/json'
  },
  data: {
    // ✅ 只需要提供这些字段
    name: '张三',
    phone: '13900000000',
    age: 25,
    gender: 'female',
    jobType: 'yuer'
  },
  success: (res) => {
    if (res.data.success) {
      console.log('✅ 注册成功！');
      console.log('简历ID:', res.data.data.id);
      // 后端已自动设置：
      // - userId: null (表示自助注册)
      // - leadSource: 'self-registration'
      // - status: 'draft'
    }
  }
});
```

### ❌ 错误做法

```javascript
// ❌ 错误：尝试传递创建来源相关字段
wx.request({
  url: 'https://crm.andejiazheng.com/api/resumes/miniprogram/self-register',
  method: 'POST',
  data: {
    name: '张三',
    phone: '13900000000',
    age: 25,
    gender: 'female',
    jobType: 'yuer',
    
    // ❌ 这些字段会被后端忽略
    userId: 'some-user-id',
    createdBy: 'self-registration',
    leadSource: 'self-registration'
  }
});
```

---

## 📊 后端自动处理

### 你发送的数据
```javascript
{
  name: '张三',
  phone: '13900000000',
  age: 25,
  gender: 'female',
  jobType: 'yuer'
}
```

### 后端保存到数据库
```javascript
{
  _id: ObjectId('...'),
  name: '张三',
  phone: '13900000000',
  age: 25,
  gender: 'female',
  jobType: 'yuer',
  
  // ⭐ 后端自动设置
  userId: null,                    // 表示自助注册
  leadSource: 'self-registration', // 线索来源
  status: 'draft',                 // 简历状态
  createdAt: '2025-10-17T...',
  updatedAt: '2025-10-17T...'
}
```

---

## 🔍 CRM端如何显示

### 简历列表

在表格中显示"创建来源"列：

| 创建来源 | 颜色 | 说明 |
|---------|------|------|
| 自助注册 | 🔵 蓝色 | userId = null |
| 员工创建 | 🟠 橙色 | userId = ObjectId |

**你的简历会显示为**: 🔵 **自助注册**（蓝色标签）

### 简历详情

在"工作信息"卡片中显示"创建来源"：
- 自助注册 → 显示"自助注册"（蓝色）
- 员工创建 → 显示"员工创建"（橙色）

---

## ⚠️ 常见错误

### 错误1：尝试传递 userId

```javascript
// ❌ 错误
data: {
  name: '张三',
  userId: 'xxx'  // ❌ 不要这样做
}

// ✅ 正确
data: {
  name: '张三'   // 只提供基本信息
}
```

**后果**: 后端会忽略你传递的 userId，自动设置为 null

### 错误2：尝试传递 leadSource

```javascript
// ❌ 错误
data: {
  name: '张三',
  leadSource: 'self-registration'  // ❌ 不要这样做
}

// ✅ 正确
data: {
  name: '张三'  // 只提供基本信息
}
```

**后果**: 后端会忽略你传递的 leadSource，自动设置为 'self-registration'

### 错误3：尝试传递 createdBy

```javascript
// ❌ 错误
data: {
  name: '张三',
  createdBy: 'self-registration'  // ❌ 不要这样做
}

// ✅ 正确
data: {
  name: '张三'  // 只提供基本信息
}
```

**后果**: 后端会忽略你传递的 createdBy 字段

---

## 🔐 安全性说明

### 为什么后端要强制设置创建来源？

1. **防止篡改**: 前端可以被修改，不能信任
2. **数据准确**: 后端是数据的最后一道防线
3. **权限控制**: 确保只有授权的操作才能改变创建来源

### 后端防护措施

✅ 自动强制设置 `userId: null`  
✅ 自动强制设置 `leadSource: 'self-registration'`  
✅ 自动强制设置 `status: 'draft'`  
✅ 删除前端传递的敏感字段  
✅ 记录异常行为日志  

---

## 📋 完整流程

```
┌─────────────────────────────────────────────────────┐
│ 1. 小程序端收集信息                                  │
│    name, phone, age, gender, jobType                │
└────────────────┬────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────┐
│ 2. 调用自助注册接口                                  │
│    POST /api/resumes/miniprogram/self-register      │
└────────────────┬────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────┐
│ 3. 后端处理                                          │
│    ✅ 验证数据                                       │
│    ✅ 检查重复                                       │
│    ✅ 强制设置 userId: null                         │
│    ✅ 强制设置 leadSource: 'self-registration'      │
│    ✅ 强制设置 status: 'draft'                      │
│    ✅ 保存到数据库                                   │
└────────────────┬────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────┐
│ 4. 返回结果                                          │
│    { success: true, data: { id: '...' } }           │
└────────────────┬────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────┐
│ 5. CRM端显示                                         │
│    🔵 自助注册（蓝色标签）                           │
└─────────────────────────────────────────────────────┘
```

---

## 📚 相关文档

### 小程序端开发者

1. **小程序端-创建来源快速参考.md** ⭐ 快速参考
2. **小程序端-集成指南.md** - 集成步骤
3. **小程序端-自助注册接口使用文档.md** - 详细文档

### CRM端开发者

1. **简历创建来源区分-正确分析.md** - 问题分析
2. **简历创建来源区分-修复完成报告.md** - 修复报告

---

## 🎯 总结

### ✅ 你需要知道的

1. **创建来源** = 谁创建的（自助 vs 员工）
2. **你的职责** = 提供基本信息
3. **后端职责** = 自动标记为自助注册
4. **CRM显示** = 蓝色标签"自助注册"

### ✅ 你需要做的

1. 调用自助注册接口
2. 提供必要的字段（name, phone, age, gender, jobType）
3. 处理响应和错误

### ❌ 你不需要做的

1. 不要传递 userId
2. 不要传递 createdBy
3. 不要传递 leadSource
4. 不要尝试手动设置创建来源

---

## 📞 常见问题

**Q: 我需要在请求中传递创建来源吗？**  
A: 不需要！后端会自动处理。

**Q: 如果我传递了 userId 会怎样？**  
A: 后端会忽略它，自动设置为 null。

**Q: 我的简历在CRM中如何显示？**  
A: 显示为蓝色标签"自助注册"。

**Q: 为什么后端要强制设置？**  
A: 防止前端篡改，确保数据准确性和安全性。

**Q: 创建来源和线索来源有什么区别？**  
A: 创建来源 = 谁创建的；线索来源 = 从什么渠道获取的。

---

**版本**: v1.0.0  
**最后更新**: 2025-10-17  
**状态**: ✅ 生产环境已部署

