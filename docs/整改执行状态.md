# 整改执行状态

## 状态说明

- `待确认`：方案已提出，尚未开始
- `进行中`：正在实施
- `已完成`：代码或文档已完成
- `已验证`：已验证通过
- `暂缓`：暂时不执行，等待依赖条件

## 总体原则

- 不跳步骤执行
- 每次只推进一类改动
- 所有兼容迁移项必须先评估小程序影响

## 任务清单

| 编号 | 任务 | 优先级 | 当前状态 | 是否影响 CRM | 是否影响小程序 | 备注 |
|---|---|---|---|---|---|---|
| A1 | 建立整改方案文档 | P0 | 已完成 | 否 | 否 | 本文档已创建 |
| A2 | 建立整改状态清单 | P0 | 已完成 | 否 | 否 | 本文档已创建 |
| A3 | 盘点所有 `@Public()` 接口用途和调用方 | P0 | 已完成 | 否 | 是 | 已形成三分类清单，后续可继续细化调用方 |
| A4 | 盘点所有测试/调试/临时接口 | P0 | 已完成 | 是 | 可能 | 已形成优先下线名单 |
| A5 | 修复爱签回调未配置公钥仍放行 | P0 | 暂缓 | 否 | 否 | 先做配置与回调链路核查，不直接改行为 |
| A6 | 下线 `clear-login-limit` 公开接口 | P0 | 已验证 | 否 | 否 | 已移除控制器公开路由，后端构建通过；服务层方法暂保留未暴露 |
| A7 | 收紧 CRM 客户详情资源级鉴权 | P0 | 已验证 | 是 | 否 | customers.controller.ts findOne 增加 canAccessCustomer + sanitizeCustomerData |
| A8 | 收紧 CRM 合同详情资源级鉴权 | P0 | 已验证 | 是 | 否 | contracts.controller.ts findOne + getEsignInfo 增加 isManagerOrAdmin 校验 |
| A9 | 核查爱签回调配置、验签入口和最近回调落库链路 | P0 | 已完成 | 否 | 否 | 已补入方案文档，结论是先不直接收紧 |
| A10 | 下线 `/api/contracts/test-no-auth` 测试接口 | P0 | 已验证 | 否 | 否 | 已移除控制器公开路由，后端构建通过 |
| A11 | 下线 `contracts-test` 测试控制器整组 | P0 | 已验证 | 否 | 否 | 已删除测试控制器类，后端构建通过 |
| A12 | 下线 `/api/esign/debug-config-copy` 调试副本接口 | P0 | 已验证 | 否 | 否 | 已移除重复调试副本路由，后端构建通过 |
| A13 | 下线 `/api/esign/debug-config` 调试配置接口 | P0 | 已验证 | 否 | 否 | 已移除调试配置路由，后端构建通过 |
| A14 | 下线 `/api/esign/test-connection` 调试连接接口 | P0 | 已验证 | 否 | 否 | 已移除调试连接路由，后端构建通过 |
| A15 | 下线 `/api/contracts/esign/test-get-contract` 测试接口 | P0 | 已验证 | 否 | 否 | 已移除路由，后端构建通过 |
| A16 | 下线 `/api/follow-ups/test-populate` 测试接口 | P0 | 已验证 | 否 | 否 | 已移除路由，后端构建通过 |
| A17 | 下线 `/api/esign/test-get-contract` 测试接口 | P0 | 已验证 | 否 | 否 | 已移除路由，后端构建通过 |
| A18 | 下线 `/api/resumes/test-search-workers` 测试接口 | P0 | 已验证 | 否 | 否 | 已移除路由，后端构建通过 |
| A19 | 下线 `/api/esign/test` 测试接口 | P0 | 已验证 | 否 | 否 | 已移除路由，后端构建通过 |
| A20 | 下线 `/api/contracts/signed-callback/:contractId` 占位接口 | P0 | 已验证 | 否 | 否 | 已移除未实现占位路由，后端构建通过 |
| B1 | 盘点小程序当前使用的客户 API | P1 | 已完成 | 否 | 是 | 已形成客户链路依赖清单 |
| B2 | 盘点小程序当前使用的合同 API | P1 | 已完成 | 否 | 是 | 已形成合同链路依赖与重复能力清单 |
| B3 | 盘点小程序当前使用的简历 API | P1 | 已完成 | 否 | 是 | 已形成简历链路依赖清单 |
| B4 | 盘点小程序当前使用的保险 API | P1 | 已完成 | 否 | 是 | 已形成保险链路依赖清单 |
| B5 | 制定公开接口迁移到小程序专用接口方案 | P1 | 已完成 | 否 | 是 | 结论见整改方案文档§12；管理端3个公开重复接口已有对应小程序专用路径，待确认小程序未调用旧路径后可下线 |
| B6 | 盘点小程序用户与认证链路依赖 | P1 | 已完成 | 否 | 是 | 已补入方案文档 |
| C1 | 恢复 `ESignController` 默认鉴权，保留必要公开白名单 | P0 | 已验证 | 是 | 可能 | 取消注释 @UseGuards(JwtAuthGuard)；移除 test-notification @Public()；保留 callback/templates/template/data @Public() |
| C2 | 收紧 DTO 校验策略并拆分迁移计划 | P1 | 已完成 | 是 | 是 | 当前 main.ts: whitelist=true, forbidNonWhitelisted=false, skipMissingProperties=true；共 64 个 DTO；建议按模块分批收紧，先从无小程序依赖的模块开始；不做全局一次性切换 |
| C3 | 清理 `temp` 占位链路 | P1 | 已完成 | 是 | 是 | 触发点：contracts.service.ts:297-332；当 customerId/workerId/createdBy='temp' 时生成随机 ObjectId（产生孤儿数据）；来源疑为历史爱签同步流程；建议先加 warn 日志监控触发频率，再决定是否改为显式报错 |
| C4 | 统一小程序接口守卫 | P1 | 已验证 | 否 | 是 | resume.controller.ts 8个路由 @UseGuards(RolesGuard) → @UseGuards(JwtAuthGuard, RolesGuard)；dashubao 和 zmdb 已正确配置；构建通过 |
| D1 | 优化客户列表 N+1 查询 | P2 | 已验证 | 是 | 否 | customers.service.ts findAll 非过滤路径：Promise.all(N × find) → 1次批量 find+populate；customers.service.ts 聚合路径已有 $addFields 内联计算；构建通过 |
| D2 | 前端路由单源化 | P2 | 已验证 | 是 | 否 | 删除废弃的 router.tsx（349行，未被任何文件 import）；main.tsx 只渲染 App.tsx；前端 tsc 零错误 |
| D3 | 模块边界梳理与分层 | P2 | 待确认 | 是 | 是 | 中期改造 |
| D4 | 清理仓库历史遗留文件与目录 | P3 | 已验证 | 否 | 否 | 已删除：小程序H5文件_旧版备份_已废弃/（2.3MB）、.atomic-backups/、爱签SDK/（13MB）、frontend/public/miniprogram/*.backup；backups/（3.6GB部署快照）和网站证书暂保留 |
| D5 | 补充合同/爱签/保险/权限测试 | P1 | 已验证 | 是 | 是 | 已补3组测试(18个用例全通过)：esign-callback.service.spec(验签+公钥缺失)、contracts.controller.auth.spec(A8资源鉴权)、customers.controller.auth.spec(A7资源鉴权逻辑) |

## 本轮已完成

- 完成项目全盘结构和主链路抽查
- 完成小程序专用接口入口检索
- 完成风险分级与整改顺序设计
- 完成整改方案文档和状态文档落盘
- 完成公开接口/小程序接口/测试接口三分类
- 完成爱签回调配置与链路核查
- 完成小程序客户/合同/简历/保险/认证依赖清单
- 完成全部 P0 安全收口（A6-A20、C1、A7、A8）
- 完成 C4 小程序守卫统一（resume 8个路由）
- 完成 D1 客户列表 N+1 消除
- 完成 D4 历史遗留文件清理
- 完成 B5/C2/C3 分析并落文档

## 剩余待执行任务

| 编号 | 任务 | 优先级 | 建议行动 |
|---|---|---|---|
| D2 | 前端路由单源化 | P2 | 可独立推进，先检查两套路由定义差异 |
| D3 | 模块边界梳理与分层 | P2 | 中期改造，需单独评估 |
| D5 | 补充合同/爱签/保险/权限测试 | P1 | 重要但工作量大，建议伴随后续迭代同步推进 |
| A5 | 修复爱签回调未配置公钥仍放行 | P0（暂缓） | 等待现网回调链路确认后再收紧 |
