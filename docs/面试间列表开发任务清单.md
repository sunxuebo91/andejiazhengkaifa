# ✅ 面试间列表功能开发任务清单

## 📅 开发计划

**预计时间**: 6-8 小时（1 天）  
**开发日期**: 明天  
**开发顺序**: 后端 → 前端 → 测试

---

## 🔧 第一阶段：后端开发（3-4 小时）

### Step 1: 创建模块目录结构（5分钟）

```bash
cd backend/src/modules
mkdir -p interview/models interview/dto
```

- [ ] 创建 `interview` 目录
- [ ] 创建 `models` 子目录
- [ ] 创建 `dto` 子目录

---

### Step 2: 创建数据模型（30分钟）

#### 2.1 创建 Participant Schema

**文件**: `backend/src/modules/interview/models/interview-room.entity.ts`

- [ ] 定义 `Participant` 类
- [ ] 添加字段：`userId`, `userName`, `role`, `identity`, `joinedAt`, `leftAt`
- [ ] 创建 `ParticipantSchema`

#### 2.2 创建 InterviewRoom Entity

**同一文件继续**

- [ ] 定义 `InterviewRoom` 类
- [ ] 添加字段：`roomId`, `roomName`, `hostUserId`, `hostName`, `hostZegoUserId`
- [ ] 添加字段：`status`, `createdAt`, `endedAt`, `duration`, `participants`
- [ ] 添加装饰器：`@Schema`, `@Prop`
- [ ] 创建 `InterviewRoomSchema`
- [ ] 添加索引：`hostUserId + status`, `createdAt`

**参考**: 开发文档第 9-60 行

---

### Step 3: 创建 DTO（20分钟）

#### 3.1 CreateRoomDto

**文件**: `backend/src/modules/interview/dto/create-room.dto.ts`

- [ ] 定义 `CreateRoomDto` 类
- [ ] 添加字段：`roomId`, `roomName`, `hostName`, `hostZegoUserId`
- [ ] 添加验证装饰器：`@IsString()`, `@IsNotEmpty()`

#### 3.2 QueryRoomsDto

**文件**: `backend/src/modules/interview/dto/query-rooms.dto.ts`

- [ ] 定义 `QueryRoomsDto` 类
- [ ] 添加字段：`status`, `page`, `pageSize`, `search`
- [ ] 添加验证装饰器：`@IsOptional()`, `@IsEnum()`, `@IsInt()`, `@Min()`
- [ ] 添加类型转换：`@Type(() => Number)`

#### 3.3 EndRoomDto

**文件**: `backend/src/modules/interview/dto/end-room.dto.ts`

- [ ] 定义 `EndRoomDto` 类
- [ ] 添加字段：`roomId`
- [ ] 添加验证装饰器

**参考**: 开发文档第 75-144 行

---

### Step 4: 实现 Service（60分钟）

**文件**: `backend/src/modules/interview/interview.service.ts`

- [ ] 创建 `InterviewService` 类
- [ ] 注入依赖：`@InjectModel(InterviewRoom)`, `ZegoService`

#### 4.1 实现 createRoom()

- [ ] 创建数据库记录
- [ ] 设置 `hostUserId` 为 `new Types.ObjectId(userId)`
- [ ] 初始化 `participants` 数组（包含主持人）
- [ ] 调用 `zegoService.createRoom()`
- [ ] 返回创建的房间

#### 4.2 实现 findByHostUserId()

- [ ] 构建查询条件：`hostUserId === userId`
- [ ] 添加 `status` 过滤（如果提供）
- [ ] 添加 `search` 过滤（如果提供）
- [ ] 执行分页查询：`find()`, `sort()`, `skip()`, `limit()`
- [ ] 统计总数：`countDocuments()`
- [ ] 返回列表和分页信息

#### 4.3 实现 findOne()

- [ ] 根据 `roomId` 查询
- [ ] 检查是否存在（404）
- [ ] 验证所有权：`hostUserId === userId`（403）
- [ ] 返回房间详情

#### 4.4 实现 endRoom()

- [ ] 根据 `roomId` 查询
- [ ] 检查是否存在（404）
- [ ] 验证所有权（403）
- [ ] 检查是否已结束
- [ ] 更新状态：`status = 'ended'`
- [ ] 设置 `endedAt` 和计算 `duration`
- [ ] 保存到数据库
- [ ] 调用 `zegoService.dismissRoom()`
- [ ] 返回更新后的房间

#### 4.5 实现 checkRoomStatus()

- [ ] 根据 `roomId` 查询数据库
- [ ] 检查是否存在
- [ ] 验证所有权
- [ ] 检查数据库状态
- [ ] 调用 `zegoService.checkRoom()` 检查内存状态
- [ ] 返回综合状态

**参考**: 开发文档第 166-371 行

---

### Step 5: 实现 Controller（40分钟）

**文件**: `backend/src/modules/interview/interview.controller.ts`

- [ ] 创建 `InterviewController` 类
- [ ] 添加装饰器：`@Controller('interview')`, `@UseGuards(JwtAuthGuard)`
- [ ] 注入 `InterviewService`

#### 5.1 实现 createRoom()

- [ ] 添加装饰器：`@Post('rooms')`
- [ ] 从 `req.user.userId` 获取当前用户ID
- [ ] 调用 `interviewService.createRoom()`
- [ ] 返回统一响应格式
- [ ] 添加错误处理

#### 5.2 实现 getRooms()

- [ ] 添加装饰器：`@Get('rooms')`
- [ ] 从 JWT 获取 `userId`
- [ ] 调用 `interviewService.findByHostUserId()`
- [ ] 返回列表数据

#### 5.3 实现 getRoomDetail()

- [ ] 添加装饰器：`@Get('rooms/:roomId')`
- [ ] 获取路径参数 `roomId`
- [ ] 调用 `interviewService.findOne()`
- [ ] 处理 403/404 错误

#### 5.4 实现 endRoom()

- [ ] 添加装饰器：`@Post('rooms/:roomId/end')`
- [ ] 调用 `interviewService.endRoom()`
- [ ] 返回结果

#### 5.5 实现 checkRoomStatus()

- [ ] 添加装饰器：`@Get('rooms/:roomId/status')`
- [ ] 调用 `interviewService.checkRoomStatus()`
- [ ] 返回状态信息

**参考**: 开发文档第 376-520 行

---

### Step 6: 创建 Module（10分钟）

**文件**: `backend/src/modules/interview/interview.module.ts`

- [ ] 创建 `InterviewModule` 类
- [ ] 导入 `MongooseModule.forFeature([InterviewRoom])`
- [ ] 导入 `forwardRef(() => ZegoModule)`
- [ ] 注册 `InterviewController`
- [ ] 注册 `InterviewService`
- [ ] 导出 `InterviewService`

**参考**: 开发文档第 525-542 行

---

### Step 7: 注册模块（5分钟）

**文件**: `backend/src/app.module.ts`

- [ ] 导入 `InterviewModule`
- [ ] 在 `imports` 数组中添加 `InterviewModule`

**参考**: 开发文档第 547-560 行

---

### Step 8: 整合 ZEGO Service（30分钟）

#### 8.1 修改 ZegoController

**文件**: `backend/src/modules/zego/zego.controller.ts`

- [ ] 注入 `InterviewService`
- [ ] 在 `generateToken()` 中，如果是主持人，调用 `interviewService.createRoom()`

#### 8.2 修改 ZegoModule

**文件**: `backend/src/modules/zego/zego.module.ts`

- [ ] 导入 `forwardRef(() => InterviewModule)`

#### 8.3 增强 ZegoService

**文件**: `backend/src/modules/zego/zego.service.ts`

- [ ] 添加 `checkRoom(roomId)` 方法
- [ ] 检查内存中的房间状态
- [ ] 返回 `{ exists, canJoin }`

**参考**: 开发文档第 565-640 行

---

### Step 9: 后端测试（20分钟）

- [ ] 启动后端：`cd backend && npm run start:dev`
- [ ] 检查是否有编译错误
- [ ] 使用 Postman 测试 API
  - [ ] 登录获取 Token
  - [ ] 测试创建面试间
  - [ ] 测试获取列表
  - [ ] 测试结束面试间
  - [ ] 测试权限隔离

---

## 🎨 第二阶段：前端开发（3-4 小时）

### Step 10: 创建目录结构（5分钟）

```bash
cd frontend/src
mkdir -p pages/interview/components
mkdir -p types
mkdir -p services
```

- [ ] 创建 `pages/interview/components` 目录
- [ ] 确保 `types` 目录存在
- [ ] 确保 `services` 目录存在

---

### Step 11: 创建类型定义（10分钟）

**文件**: `frontend/src/types/interview.types.ts`

- [ ] 定义 `Participant` 接口
- [ ] 定义 `InterviewRoom` 接口
- [ ] 定义 `RoomListResponse` 接口
- [ ] 定义 `RoomStatusResponse` 接口

**参考**: 开发文档第 598-625 行

---

### Step 12: 创建 API Service（15分钟）

**文件**: `frontend/src/services/interview.service.ts`

- [ ] 导入 `apiService`
- [ ] 实现 `createRoom()`
- [ ] 实现 `getRooms()`
- [ ] 实现 `getRoomDetail()`
- [ ] 实现 `endRoom()`
- [ ] 实现 `checkRoomStatus()`

**参考**: 开发文档第 630-670 行

---

### Step 13: 创建状态徽章组件（10分钟）

**文件**: `frontend/src/pages/interview/components/RoomStatusBadge.tsx`

- [ ] 创建 `RoomStatusBadge` 组件
- [ ] 接收 `status` prop
- [ ] 根据状态显示不同颜色的 Tag
  - [ ] `active` → 绿色 "🟢 进行中"
  - [ ] `ended` → 灰色 "🔴 已结束"

**参考**: 开发文档第 675-690 行

---

### Step 14: 创建房间卡片组件（40分钟）

**文件**: `frontend/src/pages/interview/components/RoomCard.tsx`

- [ ] 导入依赖：`Card`, `Button`, `Space`, `Descriptions`, `Modal`, `dayjs`
- [ ] 定义 `RoomCardProps` 接口
- [ ] 创建 `RoomCard` 组件

#### 14.1 实现辅助函数

- [ ] `formatDuration()` - 格式化时长（秒 → 小时分钟）
- [ ] `getActiveDuration()` - 计算进行中的时长

#### 14.2 实现 UI 布局

- [ ] Card 标题：状态徽章 + 房间名称
- [ ] Card extra：房间号（简化显示）
- [ ] Descriptions：创建时间、持续时长、结束时间、参与者
- [ ] 操作按钮区域

#### 14.3 实现交互逻辑

- [ ] `handleEnd()` - 确认结束面试（Modal.confirm）
- [ ] 根据状态显示不同按钮
  - [ ] `active`：重新进入、结束面试、查看详情
  - [ ] `ended`：查看详情

**参考**: 开发文档第 695-852 行

---

### Step 15: 创建列表页面（60分钟）

**文件**: `frontend/src/pages/interview/InterviewRoomList.tsx`

- [ ] 导入依赖：`Card`, `Tabs`, `Input`, `Pagination`, `Empty`, `Spin`, `message`
- [ ] 导入组件：`RoomCard`
- [ ] 导入服务：`interviewService`

#### 15.1 定义状态

- [ ] `loading` - 加载状态
- [ ] `rooms` - 房间列表
- [ ] `total` - 总数
- [ ] `page` - 当前页
- [ ] `pageSize` - 每页数量
- [ ] `status` - 状态筛选
- [ ] `searchText` - 搜索文本

#### 15.2 实现数据加载

- [ ] `loadRooms()` - 加载房间列表
- [ ] `useEffect` - 监听依赖变化自动加载

#### 15.3 实现交互函数

- [ ] `handleTabChange()` - 切换状态标签
- [ ] `handleSearch()` - 搜索
- [ ] `handleRejoin()` - 重新进入（先检查状态）
- [ ] `handleEnd()` - 结束面试
- [ ] `handleViewDetail()` - 查看详情
- [ ] `handleCreateRoom()` - 创建新面试间

#### 15.4 实现 UI 布局

- [ ] Card 标题 + 操作按钮（创建、刷新）
- [ ] 搜索栏
- [ ] Tabs（全部、进行中、已结束）
- [ ] 房间列表（使用 RoomCard）
- [ ] 空状态（Empty）
- [ ] 分页器（Pagination）

**参考**: 开发文档第 857-1050 行

---

### Step 16: 创建样式文件（10分钟）

**文件**: `frontend/src/pages/interview/InterviewRoomList.css`

- [ ] 设置页面容器样式
- [ ] 设置 Card 样式（圆角、阴影）
- [ ] 设置 Descriptions 样式
- [ ] 设置 Empty 样式

**参考**: 开发文档第 1055-1070 行

---

### Step 17: 配置路由（10分钟）

**文件**: `frontend/src/App.tsx` 或 `frontend/src/router.tsx`

- [ ] 导入 `InterviewRoomList` 组件
- [ ] 添加路由配置：`/interview/rooms`

**参考**: 开发文档第 1075-1082 行

---

### Step 18: 配置菜单（10分钟）

**文件**: `frontend/src/layouts/BasicLayout.tsx`

- [ ] 找到视频面试菜单配置
- [ ] 在 `children` 中添加"面试间列表"菜单项
- [ ] 设置路径：`/interview/rooms`
- [ ] 设置图标：`<UnorderedListOutlined />`

**参考**: 开发文档第 1087-1105 行

---

### Step 19: 前端测试（30分钟）

- [ ] 启动前端：`cd frontend && npm start`
- [ ] 检查是否有编译错误
- [ ] 登录系统
- [ ] 访问 `/interview/rooms`

#### 功能测试

- [ ] 列表加载正常
- [ ] 状态筛选（全部、进行中、已结束）
- [ ] 搜索功能
- [ ] 分页功能
- [ ] 创建新面试间（跳转到创建页）
- [ ] 重新进入面试间
- [ ] 结束面试间（确认弹窗）
- [ ] 查看详情
- [ ] 刷新列表

#### UI 测试

- [ ] 状态徽章显示正确
- [ ] 时长计算正确
- [ ] 参与者信息显示正确
- [ ] 空状态显示正确
- [ ] 响应式布局正常

---

## 🧪 第三阶段：集成测试（1-2 小时）

### Step 20: 完整流程测试（30分钟）

#### 测试场景 1：创建和管理面试间

- [ ] 用户A登录
- [ ] 创建面试间
- [ ] 在列表中看到新创建的面试间（状态：进行中）
- [ ] 重新进入面试间
- [ ] 结束面试间
- [ ] 状态变为"已结束"
- [ ] 查看详情，时长计算正确

#### 测试场景 2：权限隔离

- [ ] 用户A创建面试间
- [ ] 用户B登录
- [ ] 用户B的列表中看不到用户A的面试间
- [ ] 用户B尝试直接访问用户A的面试间URL（应该返回403）
- [ ] 用户A可以正常访问自己的面试间

#### 测试场景 3：搜索和筛选

- [ ] 创建多个面试间（不同状态）
- [ ] 测试状态筛选（全部、进行中、已结束）
- [ ] 测试搜索功能
- [ ] 测试分页功能

---

### Step 21: 边界测试（20分钟）

- [ ] 空列表显示
- [ ] 搜索无结果
- [ ] 分页边界（第一页、最后一页）
- [ ] 网络错误处理
- [ ] Token 过期处理
- [ ] 重复结束面试间
- [ ] 进入已结束的面试间

---

### Step 22: 性能测试（10分钟）

- [ ] 创建 20+ 个面试间
- [ ] 测试列表加载速度
- [ ] 测试分页切换速度
- [ ] 测试搜索响应速度

---

### Step 23: 浏览器兼容性测试（10分钟）

- [ ] Chrome（主要浏览器）
- [ ] Firefox
- [ ] Safari（如果有Mac）
- [ ] Edge

---

## 📝 第四阶段：文档和收尾（30分钟）

### Step 24: 代码审查（15分钟）

- [ ] 检查代码格式
- [ ] 检查命名规范
- [ ] 检查注释完整性
- [ ] 检查错误处理
- [ ] 检查类型定义

---

### Step 25: 提交代码（15分钟）

- [ ] 运行 `git status` 查看修改
- [ ] 运行 `git add .` 添加所有文件
- [ ] 运行 `git commit -m "feat: 实现面试间列表功能"`
- [ ] 运行 `git push` 推送到远程

---

## 🎉 完成检查

### 后端完成标准

- [x] 所有 Entity、DTO、Service、Controller 已创建
- [x] 所有 API 接口正常工作
- [x] 权限验证正确实施
- [x] 数据库索引已创建
- [x] 错误处理完善

### 前端完成标准

- [x] 所有组件已创建
- [x] 列表页面功能完整
- [x] 路由和菜单配置正确
- [x] UI 美观、交互流畅
- [x] 错误提示友好

### 测试完成标准

- [x] 所有功能测试通过
- [x] 权限隔离测试通过
- [x] 边界测试通过
- [x] 浏览器兼容性测试通过

---

## 🚨 常见问题和解决方案

### 问题 1：循环依赖错误

**症状**: `Cannot access 'InterviewModule' before initialization`

**解决**: 使用 `forwardRef()`

```typescript
imports: [forwardRef(() => ZegoModule)]
```

### 问题 2：JWT 获取不到 userId

**症状**: `req.user.userId` 是 `undefined`

**解决**: 检查 `jwt.strategy.ts` 的 `validate()` 方法是否返回了 `userId`

### 问题 3：权限验证失败

**症状**: 用户可以看到其他人的面试间

**解决**: 检查 Service 中的查询条件是否包含 `hostUserId` 过滤

### 问题 4：时长计算不正确

**症状**: 显示的时长与实际不符

**解决**: 检查 `duration` 单位是否为秒，前端格式化是否正确

### 问题 5：ZEGO 房间状态不同步

**症状**: 数据库显示 active，但 ZEGO 内存中已解散

**解决**: 在 `checkRoomStatus()` 中同时检查数据库和 ZEGO 状态

---

## 📚 参考文档

1. **开发文档**: `docs/面试间列表功能开发文档.md`
2. **API 文档**: `docs/面试间列表API接口文档.md`
3. **NestJS 文档**: https://docs.nestjs.com/
4. **Ant Design 文档**: https://ant.design/components/overview-cn/
5. **Day.js 文档**: https://day.js.org/

---

## 💡 开发建议

1. **按顺序开发**：先后端再前端，避免来回切换
2. **频繁测试**：每完成一个功能就测试一次
3. **及时提交**：每完成一个大步骤就提交代码
4. **保持专注**：一次只做一件事，不要跳跃
5. **遇到问题先查文档**：大部分问题文档中都有答案

---

## ⏰ 时间分配建议

| 阶段 | 时间 | 说明 |
|-----|------|------|
| 后端开发 | 3-4小时 | 核心功能实现 |
| 前端开发 | 3-4小时 | UI和交互实现 |
| 集成测试 | 1-2小时 | 全面测试 |
| 文档收尾 | 0.5小时 | 代码审查和提交 |
| **总计** | **7.5-10.5小时** | **建议分两天完成** |

---

**祝开发顺利！🚀**

如有问题，随时参考开发文档和 API 文档。

