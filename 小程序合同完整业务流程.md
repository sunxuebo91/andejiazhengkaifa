# 小程序合同完整业务流程

## 📋 目录

1. [合同创建流程](#1-合同创建流程)
2. [合同查询流程](#2-合同查询流程)
3. [合同签署流程](#3-合同签署流程)
4. [合同状态同步](#4-合同状态同步)
5. [换人合同流程](#5-换人合同流程)
6. [保险同步流程](#6-保险同步流程)
7. [完整流程图](#7-完整流程图)

---

## 1. 合同创建流程

### 📱 小程序端操作

#### 步骤1：填写合同信息
用户在小程序填写表单：
- 客户信息：姓名、手机号、身份证号、服务地址
- 阿姨信息：姓名、手机号、身份证号
- 合同信息：服务类型、开始日期、结束日期、工资、服务费

#### 步骤2：提交创建请求

```javascript
// 小程序代码
const formData = {
  customerName: "张三",
  customerPhone: "13800138000",
  customerIdCard: "110101199001011234",
  workerName: "李四",
  workerPhone: "13900139000",
  workerIdCard: "110101198001011234",
  contractType: "住家育儿嫂",
  startDate: "2026-02-20",
  endDate: "2027-02-20",
  workerSalary: 10000,
  customerServiceFee: 2700
};

const res = await wx.request({
  url: 'https://crm.andejiazheng.com/api/contracts/miniprogram/create',
  method: 'POST',
  data: formData
});

if (res.data.success) {
  const contract = res.data.data;
  console.log('合同创建成功:', contract.contractNumber);
  
  // 解析签署链接
  const signUrls = JSON.parse(contract.esignSignUrls || '[]');
  const customerSignUrl = signUrls.find(s => s.role === '甲方（客户）')?.signUrl;
  
  // 跳转到签署页面
  wx.navigateTo({
    url: `/pages/contract/sign?url=${encodeURIComponent(customerSignUrl)}`
  });
}
```

### 🔧 后端自动执行

**接口**：`POST /api/contracts/miniprogram/create`

**执行流程**：
1. ✅ 保存合同到数据库（生成合同编号）
2. ✅ 调用爱签API为甲乙双方创建账户
3. ✅ 调用爱签API创建模板合同
4. ✅ 调用爱签API添加签署方
5. ✅ 获取签署链接并保存到数据库
6. ✅ 返回完整合同信息（含签署链接）

**返回数据**：
```json
{
  "success": true,
  "message": "合同创建成功",
  "data": {
    "_id": "698fd0ea74ea0f8c1ada5b95",
    "contractNumber": "CONTRACT_1771032806041_ogwyb9k9n",
    "customerName": "张三",
    "workerName": "李四",
    "contractStatus": "signing",
    "esignContractNo": "CONTRACT_1771032806041_ogwyb9k9n",
    "esignStatus": "1",
    "esignSignUrls": "[{\"name\":\"张三\",\"mobile\":\"13800138000\",\"role\":\"甲方（客户）\",\"signUrl\":\"https://callback.asign.cn/web/short/YRBzYj799163\"},{\"name\":\"李四\",\"mobile\":\"13900139000\",\"role\":\"乙方（服务人员）\",\"signUrl\":\"https://callback.asign.cn/web/short/rmEJfq968956\"}]",
    "createdAt": "2026-02-14T01:33:30.389Z"
  }
}
```

---

## 2. 合同查询流程

### 📱 获取合同列表

**接口**：`GET /api/contracts/miniprogram/list`

**请求参数**：
```javascript
{
  page: 1,              // 页码
  limit: 10,            // 每页数量
  search: "张三",       // 搜索关键词（可选）
  syncStatus: true      // 是否同步爱签最新状态（默认true）
}
```

**小程序代码**：
```javascript
const res = await wx.request({
  url: 'https://crm.andejiazheng.com/api/contracts/miniprogram/list',
  method: 'GET',
  data: {
    page: 1,
    limit: 10,
    syncStatus: true  // 自动同步最新状态
  }
});

if (res.data.success) {
  const contracts = res.data.data.contracts;
  
  contracts.forEach(contract => {
    console.log(`${contract.customerName}: ${contract.esignStatusText}`);
    // 显示：张三: 签约中
  });
}
```

**返回数据**：
```json
{
  "success": true,
  "data": {
    "contracts": [
      {
        "contractNumber": "CONTRACT_1771032806041_ogwyb9k9n",
        "customerName": "张三",
        "workerName": "李四",
        "contractStatus": "signing",
        "esignStatus": "1",
        "esignStatusText": "签约中",
        "createdAt": "2026-02-14T01:33:30.389Z"
      }
    ],
    "total": 1,
    "page": 1,
    "limit": 10,
    "totalPages": 1
  }
}
```

### 📱 获取合同详情

**接口**：`GET /api/contracts/miniprogram/detail/:id`

**小程序代码**：
```javascript
const res = await wx.request({
  url: `https://crm.andejiazheng.com/api/contracts/miniprogram/detail/${contractId}`,
  method: 'GET'
});

if (res.data.success) {
  const contract = res.data.data;
  // 显示合同详细信息
}
```

---

## 3. 合同签署流程

### 📱 获取签署链接

**方法1：从创建接口返回的数据中获取**
```javascript
// 创建合同后直接获取
const signUrls = JSON.parse(contract.esignSignUrls || '[]');
const customerSignUrl = signUrls.find(s => s.role === '甲方（客户）')?.signUrl;
```

**方法2：调用专用接口重新获取**

**接口**：`POST /api/contracts/miniprogram/resend-sign-urls/:id`

```javascript
const res = await wx.request({
  url: `https://crm.andejiazheng.com/api/contracts/miniprogram/resend-sign-urls/${contractId}`,
  method: 'POST'
});

if (res.data.success) {
  const signUrls = res.data.data;
  // signUrls 是数组，包含所有签署方的链接
}
```

### 📱 跳转到签署页面

```javascript
// 使用 WebView 打开签署链接
wx.navigateTo({
  url: `/pages/webview/index?url=${encodeURIComponent(signUrl)}`
});
```

### 📱 查看签署进度

**接口**：`GET /api/contracts/miniprogram/signers-status/:id`

```javascript
const res = await wx.request({
  url: `https://crm.andejiazheng.com/api/contracts/miniprogram/signers-status/${contractId}`,
  method: 'GET'
});

if (res.data.success) {
  const { signers, summary } = res.data.data;
  
  console.log(`总签署人数: ${summary.totalSigners}`);
  console.log(`已签署: ${summary.signedCount}`);
  console.log(`待签署: ${summary.pendingCount}`);
  console.log(`全部完成: ${summary.allSigned}`);
  
  signers.forEach(signer => {
    console.log(`${signer.name} (${signer.role}): ${signer.signStatusText}`);
  });
}
```

**返回数据**：
```json
{
  "success": true,
  "data": {
    "contractNo": "CONTRACT_1771032806041_ogwyb9k9n",
    "contractStatus": 1,
    "contractStatusText": "签约中",
    "signers": [
      {
        "name": "张三",
        "role": "甲方（客户）",
        "phone": "13800138000",
        "signStatus": 2,
        "signStatusText": "已签约",
        "isSigned": true,
        "isPending": false,
        "isRejected": false
      },
      {
        "name": "李四",
        "role": "乙方（服务人员）",
        "phone": "13900139000",
        "signStatus": 0,
        "signStatusText": "待签约",
        "isSigned": false,
        "isPending": true,
        "isRejected": false
      }
    ],
    "summary": {
      "totalSigners": 2,
      "signedCount": 1,
      "pendingCount": 1,
      "rejectedCount": 0,
      "allSigned": false
    }
  }
}
```

---

## 4. 合同状态同步

### 📱 手动同步单个合同状态

**接口**：`POST /api/contracts/miniprogram/sync-esign-status/:id`

```javascript
const res = await wx.request({
  url: `https://crm.andejiazheng.com/api/contracts/miniprogram/sync-esign-status/${contractId}`,
  method: 'POST'
});

if (res.data.success) {
  const { oldStatus, newStatus, statusChanged } = res.data.data;

  if (statusChanged) {
    wx.showToast({
      title: `状态已更新为: ${newStatus}`,
      icon: 'success'
    });
  }
}
```

### 📱 批量同步所有合同状态

**接口**：`POST /api/contracts/miniprogram/sync-all-esign-status`

```javascript
const res = await wx.request({
  url: 'https://crm.andejiazheng.com/api/contracts/miniprogram/sync-all-esign-status',
  method: 'POST'
});

if (res.data.success) {
  const { totalContracts, syncedCount, updatedCount } = res.data.data;

  console.log(`同步完成: ${syncedCount}/${totalContracts} 个合同`);
  console.log(`状态变更: ${updatedCount} 个合同`);
}
```

### 🔄 自动同步机制

**列表查询时自动同步**：
- 调用 `GET /api/contracts/miniprogram/list?syncStatus=true` 时
- 后端自动查询爱签API获取最新状态
- 实时返回最新状态，无需手动同步

---

## 5. 换人合同流程

### 📱 创建换人合同

当客户需要更换阿姨时，使用换人合同功能：

**接口**：`POST /api/contracts/miniprogram/change-worker/:originalContractId`

**小程序代码**：
```javascript
// 步骤1：检查客户是否有现有合同
const checkRes = await wx.request({
  url: 'https://crm.andejiazheng.com/api/contracts/miniprogram/check-existing-contract',
  method: 'POST',
  data: {
    customerPhone: "13800138000"
  }
});

if (checkRes.data.data.hasContract) {
  const originalContract = checkRes.data.data.contract;

  // 步骤2：创建换人合同
  const newWorkerData = {
    workerName: "王五",           // 新阿姨姓名
    workerPhone: "13700137000",  // 新阿姨手机号
    workerIdCard: "110101197001011234",
    workerSalary: 11000,
    // 客户信息自动从原合同复制
  };

  const res = await wx.request({
    url: `https://crm.andejiazheng.com/api/contracts/miniprogram/change-worker/${originalContract._id}`,
    method: 'POST',
    data: newWorkerData
  });

  if (res.data.success) {
    const newContract = res.data.data;
    console.log('换人合同创建成功:', newContract.contractNumber);

    // 原合同自动标记为 replaced（已替换）
    // 新合同状态为 signing（签约中）
  }
}
```

**换人合同特点**：
- ✅ 自动复制原合同的客户信息、服务地址等
- ✅ 原合同状态自动变更为 `replaced`（已替换）
- ✅ 新合同关联原合同ID（`replacesContractId`）
- ✅ 原合同记录新合同ID（`replacedByContractId`）
- ✅ 新合同自动创建爱签签约流程

---

## 6. 保险同步流程

### 📱 手动触发保险同步

**接口**：`POST /api/contracts/miniprogram/sync-insurance/:id`

```javascript
const res = await wx.request({
  url: `https://crm.andejiazheng.com/api/contracts/miniprogram/sync-insurance/${contractId}`,
  method: 'POST'
});

if (res.data.success) {
  const { insuranceStatus, policyNumber } = res.data.data;

  if (insuranceStatus === 'success') {
    wx.showToast({
      title: '保险同步成功',
      icon: 'success'
    });
  }
}
```

**自动同步机制**：
- 合同签署完成后（状态变为 `active`）
- 后端自动触发保险同步
- 调用大数保API为阿姨购买保险

---

## 7. 完整流程图

### 🎯 标准合同流程

```
1. 用户填写表单
   ↓
2. 调用创建接口 (POST /api/contracts/miniprogram/create)
   ↓
3. 后端自动执行：
   - 保存合同到数据库
   - 调用爱签API创建合同
   - 添加签署方
   - 获取签署链接
   ↓
4. 返回合同信息（含签署链接）
   ↓
5. 小程序跳转到签署页面
   ↓
6. 用户完成签署（短信验证）
   ↓
7. 爱签回调通知后端
   ↓
8. 后端更新合同状态为 active
   ↓
9. 自动触发保险同步
   ↓
10. 合同生效，流程完成
```

### 🔄 换人合同流程

```
1. 检查客户是否有现有合同
   ↓
2. 调用换人接口 (POST /api/contracts/miniprogram/change-worker/:id)
   ↓
3. 后端自动执行：
   - 标记原合同为 replaced
   - 创建新合同（复制客户信息）
   - 调用爱签API创建新合同
   - 添加签署方
   ↓
4. 返回新合同信息
   ↓
5. 用户签署新合同
   ↓
6. 新合同生效，原合同失效
```

---

## 📊 状态流转图

### 合同状态（contractStatus）

```
draft (草稿)
  ↓ 发起签署
signing (签约中)
  ↓ 全部签署完成
active (已签约/生效中)
  ↓ 换人
replaced (已替换)

或

signing (签约中)
  ↓ 撤销/作废
cancelled (已终止)
```

### 爱签状态（esignStatus）

```
"0" (等待签约) → "1" (签约中) → "2" (已签约)
                    ↓
                "6" (作废) / "7" (撤销)
```

---

## 🎯 一句话总结

**小程序合同完整业务流程包括：① 创建合同（调用 `POST /api/contracts/miniprogram/create` 提交甲乙双方信息，后端自动完成爱签合同创建、签署方添加、签署链接获取，返回包含签署链接的合同对象）→ ② 签署合同（小程序解析 `esignSignUrls` 字段跳转到爱签签署页面，用户完成短信验证后签署）→ ③ 状态同步（调用 `GET /api/contracts/miniprogram/list?syncStatus=true` 自动同步爱签最新状态，或调用 `POST /api/contracts/miniprogram/sync-esign-status/:id` 手动同步单个合同）→ ④ 查看进度（调用 `GET /api/contracts/miniprogram/signers-status/:id` 查看每个签署方的签署状态和进度）→ ⑤ 合同生效（全部签署完成后状态自动变更为 `active`，后端自动触发保险同步）；特殊场景包括换人合同（调用 `POST /api/contracts/miniprogram/change-worker/:originalContractId` 创建新合同并自动标记原合同为 `replaced`）和保险同步（调用 `POST /api/contracts/miniprogram/sync-insurance/:id` 手动触发或签署完成后自动触发），所有接口均为 RESTful 风格，支持实时状态同步，确保小程序、CRM端、爱签平台三方状态完全一致。**

