# 小程序简历接口 - 快速调用指南

## 🎯 一句话总结

**小程序端直接用 `wx.request` 调用 `https://crm.andejiazheng.com/api/resumes/public/list` 获取简历列表，调用 `https://crm.andejiazheng.com/api/resumes/public/{id}` 获取简历详情，无需登录认证，返回完整数据。**

---

## 📋 接口信息

| 接口 | 完整URL | 方法 | 认证 |
|------|---------|------|------|
| 简历列表 | `https://crm.andejiazheng.com/api/resumes/public/list` | GET | ❌ 无需 |
| 简历详情 | `https://crm.andejiazheng.com/api/resumes/public/{id}` | GET | ❌ 无需 |

---

## 🚀 快速开始

### 1. 获取简历列表

```javascript
// 方式一：直接调用（最简单）
wx.request({
  url: 'https://crm.andejiazheng.com/api/resumes/public/list',
  method: 'GET',
  success(res) {
    console.log('简历列表:', res.data);
  }
});

// 方式二：带参数调用
wx.request({
  url: 'https://crm.andejiazheng.com/api/resumes/public/list?page=1&pageSize=20&jobType=yuexin',
  method: 'GET',
  success(res) {
    console.log('简历列表:', res.data);
  }
});
```

**支持的查询参数：**
- `page`: 页码（默认1）
- `pageSize`: 每页数量（默认10，最大100）
- `keyword`: 搜索关键词
- `jobType`: 工种筛选（yuexin/yuesao/baomu等）
- `maxAge`: 最大年龄
- `nativePlace`: 籍贯
- `ethnicity`: 民族

**响应示例：**
```json
{
  "success": true,
  "data": {
    "items": [...],
    "total": 100,
    "page": 1,
    "pageSize": 10,
    "totalPages": 10
  },
  "message": "获取简历列表成功"
}
```

---

### 2. 获取简历详情

```javascript
// 直接调用
const resumeId = '66e2f4af8b1234567890abcd';
wx.request({
  url: `https://crm.andejiazheng.com/api/resumes/public/${resumeId}`,
  method: 'GET',
  success(res) {
    console.log('简历详情:', res.data);
  }
});
```

**响应示例：**
```json
{
  "success": true,
  "data": {
    "id": "66e2f4af8b1234567890abcd",
    "name": "张三",
    "phone": "13800138000",
    "age": 35,
    "gender": "female",
    "jobType": "yuexin",
    "skills": ["chanhou", "yuying"],
    "photoUrls": [...],
    "certificates": [...],
    ...
  },
  "message": "获取简历详情成功"
}
```

---

## 💡 封装建议

```javascript
// utils/resume-api.js
const BASE_URL = 'https://crm.andejiazheng.com/api';

class ResumeAPI {
  // 获取简历列表
  async getList(params = {}) {
    const query = new URLSearchParams(params).toString();
    const res = await wx.request({
      url: `${BASE_URL}/resumes/public/list?${query}`,
      method: 'GET'
    });
    return res.data;
  }

  // 获取简历详情
  async getDetail(id) {
    const res = await wx.request({
      url: `${BASE_URL}/resumes/public/${id}`,
      method: 'GET'
    });
    return res.data;
  }
}

export default new ResumeAPI();
```

**使用示例：**
```javascript
import resumeAPI from '@/utils/resume-api';

// 获取列表
const list = await resumeAPI.getList({ page: 1, pageSize: 20, jobType: 'yuexin' });

// 获取详情
const detail = await resumeAPI.getDetail('66e2f4af8b1234567890abcd');
```

---

## ⚠️ 注意事项

1. **无需认证**：这两个接口是公开的，不需要传递 `Authorization` 头
2. **完整数据**：返回的是完整数据，包含手机号、身份证等敏感信息
3. **生产环境**：请使用 `https://crm.andejiazheng.com/api` 作为基础URL
4. **错误处理**：请检查 `response.data.success` 字段判断请求是否成功

---

## 📚 详细文档

完整的API文档请参考：`backend/docs/小程序简历API-列表和详情.md`

