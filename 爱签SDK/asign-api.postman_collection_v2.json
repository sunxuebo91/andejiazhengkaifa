{
	"info": {
		"_postman_id": "b911c5e2-8471-4b88-bd27-24f74453506d",
		"name": "asign-api",
		"schema": "https://schema.getpostman.com/json/collection/v2.0.0/collection.json",
		"_exporter_id": "29261621"
	},
	"item": [
		{
			"name": "实名认证接口",
			"item": [
				{
					"name": "个人用户认证",
					"item": [
						{
							"name": "个人实名认证网页版",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.authTypeList = []; // 选择显示给用户的页面认证方式\r",
											"    param.realName = \"\"; // 姓名\r",
											"    param.idCardNo = ''; // 身份证号\r",
											"    param.mobile = ''; // 手机号\r",
											"    param.bankCard = ''; // 银行卡号\r",
											"    param.redirectUrl = ''; // 认证完成后同步跳转地址\r",
											"    param.notifyUrl = ''; // 认证完成后异步通知地址\r",
											"    param.bizId = ''; // 接入商自定义业务编号\r",
											"    param.faceAuthMode = ''; // 人脸识别方式\r",
											"    param.resubmit = 0; // 认证失败后是否允许重新提交\r",
											"    param.showResult = 1; // 认证结束后是否展示结果页\r",
											"    param.mobileRequired = 0; // 是否强制采集手机号\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/auth/person/identifyUrl"
							},
							"response": []
						},
						{
							"name": "个人运营商三要素认证",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.realName = ''; // 姓名\r",
											"    param.idCardNo = ''; // 身份证号\r",
											"    param.mobile = ''; // 手机号\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/auth/person/mobile3"
							},
							"response": []
						},
						{
							"name": "个人银行卡四要素认证",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.realName = ''; // 姓名\r",
											"    param.idCardNo = ''; // 身份证号\r",
											"    param.bankCard = ''; // 银行卡号\r",
											"    param.mobile = ''; // 手机号\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/auth/person/bankCard4"
							},
							"response": []
						},
						{
							"name": "个人人脸活体认证",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.realName = ''; // 姓名\r",
											"    param.idCardNo = ''; // 身份证号\r",
											"    param.redirectUrl = ''; // 人脸认证结果回调通知URL\r",
											"    param.faceAuthMode = ''; // 认证渠道\r",
											"    param.bizId = ''; // 自定义业务ID\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/auth/person/face"
							},
							"response": []
						},
						{
							"name": "个人意愿核身认证（双录）",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.realName = ''; // 姓名\r",
											"    param.idCardNo = ''; // 身份证号\r",
											"    param.question = ''; // 意愿核身播报问题\r",
											"    param.answer = ''; // 用户回答问题\r",
											"    param.redirectUrl = ''; // 人脸认证结果回调通知URL\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/auth/person/willFace"
							},
							"response": []
						}
					]
				},
				{
					"name": "企业用户认证",
					"item": [
						{
							"name": "企业实名认证网页版",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.authTypeList = []; // 选择显示给办理人的页面认证方式\r",
											"    param.companyAuthTypeList = []; // 选择显示的公司认证方式\r",
											"    param.companyName = ''; // 预填企业名称\r",
											"    param.creditCode = ''; // 预填社会统一信用代码号\r",
											"    param.legalPersonName = ''; // 预填法人姓名\r",
											"    param.legalPersonIdCardNo = ''; // 预填法人身份证号\r",
											"    param.agentName = ''; // 预填办理人姓名\r",
											"    param.agentIdCardNo = ''; // 预填办理人身份证\r",
											"    param.immutableInfoList = []; // 指定页面不可修改参数\r",
											"    param.redirectUrl = ''; // 认证完成后同步跳转地址\r",
											"    param.notifyUrl = ''; // 认证完成后异步通知地址\r",
											"    param.bizId = ''; // 接入商自定义业务编号\r",
											"    param.faceAuthMode = ''; // 人脸识别方式\r",
											"    param.showResult = 1; // 认证结束后是否展示结果页\r",
											"    param.mobileRequired = 0; // 是否强制采集手机号\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/auth/company/identifyUrl"
							},
							"response": []
						},
						{
							"name": "【法人认证】运营商三要素认证",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.companyName = ''; // 企业名称\r",
											"    param.creditCode = ''; // 社会统一信用代码\r",
											"    param.realName = ''; // 法人姓名\r",
											"    param.idCardNo = ''; // 法人身份证号\r",
											"    param.mobile = ''; // 法人手机号（限中国大陆11位手机号）\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/auth/company/mobile3"
							},
							"response": []
						},
						{
							"name": "【法人认证】银行卡四要素认证",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.companyName = ''; // 企业名称\r",
											"    param.creditCode = ''; // 社会统一信用代码\r",
											"    param.realName = ''; // 法人姓名\r",
											"    param.idCardNo = ''; // 法人身份证号\r",
											"    param.bankCard = ''; // 法人银行卡号（仅限印有“银联”字样的银行卡）\r",
											"    param.mobile = ''; // 法人手机号（限中国大陆11位手机号）\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/auth/company/bankCard4"
							},
							"response": []
						},
						{
							"name": "【法人认证】人脸活体认证",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.companyName = ''; // 企业名称\r",
											"    param.creditCode = ''; // 社会统一信用代码\r",
											"    param.realName = ''; // 法人姓名\r",
											"    param.idCardNo = ''; // 法人身份证号\r",
											"    param.redirectUrl = ''; //人脸认证结果回调通知URL\r",
											"    param.faceAuthMode = ''; // 认证渠道\r",
											"    param.bizId = ''; // 接入商自定义业务ID\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/auth/company/face"
							},
							"response": []
						},
						{
							"name": "【创建认证流程】企业核身认证",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.companyName = ''; // 企业名称\r",
											"    param.creditCode = ''; // 社会统一信用代码\r",
											"    param.realName = ''; // 法人姓名\r",
											"    param.idCardNo = ''; // 法人身份证号\r",
											"    param.mobile = ''; // 通知手机号\r",
											"    param.agentName = ''; // 办理人姓名\r",
											"    param.agentIdCardNo = ''; // 办理人身份证号\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/verify/company/ent3"
							},
							"response": []
						},
						{
							"name": "【打款认证】发起打款认证",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.serialNo = ''; // 前置流水号\r",
											"    param.bank = ''; // 对公账户开户行总行名称\r",
											"    param.bankCard = ''; // 对公账户号\r",
											"    param.cnaps = ''; // 开户行联行号\r",
											"    param.province = ''; // 对公账户开户行所在省份名称\r",
											"    param.city = ''; // 对公账户开户行所在城市名称\r",
											"    param.subbranch = ''; // 对公账户开户行支行/分行名称全称\r",
											"    param.mobile = ''; // 手机号，用于对公打款短信通知\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/auth/company/transfer"
							},
							"response": []
						},
						{
							"name": "【打款认证】查询打款进度",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.serialNo = ''; // 打款流水号\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/auth/company/transferProcess"
							},
							"response": []
						},
						{
							"name": "【打款认证】打款金额校验",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.serialNo = ''; // 打款流水号\r",
											"    param.amount = ''; // 打款金额，精确到两位小数\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/auth/company/verifyTransferAmount"
							},
							"response": []
						},
						{
							"name": "【反向打款认证】发起反向打款",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.serialNo = ''; // 前置流水号\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/auth/company/reverseTransfer"
							},
							"response": []
						},
						{
							"name": "【反向打款认证】查询反向打款进度",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.serialNo = ''; // 反向打款流水号\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/auth/company/reverseTransferProcess"
							},
							"response": []
						},
						{
							"name": "【授权书认证】法人核身",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.serialNo = ''; // 前置流水号\r",
											"    param.mobile = ''; // 用于接收法人授权书签署短信的手机号\r",
											"    param.notifyUrl = ''; // 授权书签署完成异步通知地址\r",
											"    param.redirectUrl = ''; // 授权书签署同步跳转地址\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/auth/company/authorization"
							},
							"response": []
						},
						{
							"name": "【授权书认证】获取签署链接",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.serialNo = ''; // 认证流水号\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/auth/company/authorizationUrl"
							},
							"response": []
						},
						{
							"name": "【授权书认证】查询认证结果",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.serialNo = ''; // 认证流水号\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/auth/company/authorizationResult"
							},
							"response": []
						}
					]
				},
				{
					"name": "历史接口",
					"item": [
						{
							"name": "个人用户认证",
							"item": [
								{
									"name": "个人用户认证网页版",
									"event": [
										{
											"listen": "prerequest",
											"script": {
												"exec": [
													"// 组装请求参数\r",
													"function getBizData() {\r",
													"    const param = {};\r",
													"    param.name = ''; // 姓名\r",
													"    param.idCard = ''; // 身份证\r",
													"    param.identifyType = ''; // 实名认证类型\r",
													"    param.needIdentifyTwo = 0; // 是否需要二要素认证\r",
													"    param.notifyUrl = ''; // 回调地址\r",
													"    param.faceAuthMode = ''; // 人脸识别方式\r",
													"    return param;\r",
													"}\r",
													"// 获取除文件外其他参数\r",
													"const bizData = getBizData();\r",
													"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
													"// 将请求数据bizData按键排序并转为字符串\r",
													"const jsonString = JSON.stringify(sortJSON(bizData));\r",
													"console.log(\"排序后参数：\" + jsonString);\r",
													"pm.variables.set('bizData', jsonString);\r",
													"// 获取时间戳并添加到表单中\r",
													"const timestamp = Date.now() + 1000 * 60 * 10;\r",
													"console.log('时间戳：' + timestamp);\r",
													"pm.variables.set('timestamp', timestamp);\r",
													"// 生成签名并添加到请求头中\r",
													"const sign = signature(jsonString, timestamp);\r",
													"console.log(\"签名：\" + sign);\r",
													"pm.variables.set('sign', sign);\r",
													"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
													"\r",
													"// 签名\r",
													"function signature(jsonString, timestamp) {\r",
													"    const signString = jsonString + \r",
													"        CryptoJS.MD5(jsonString) + \r",
													"        pm.variables.get('appId') + \r",
													"        timestamp;\r",
													"    eval(pm.variables.get('forgeJS'))\r",
													"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
													"    const md = forge.md.sha1.create();\r",
													"    md.update(signString, 'utf8');\r",
													"    return forge.util.encode64(privKey.sign(md));\r",
													"}\r",
													"\r",
													"// json排序\r",
													"function sortJSON(obj) {\r",
													"    if(Array.isArray(obj)) {\r",
													"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
													"            for(let i = 0; i < obj.length; i++) {\r",
													"                obj[i] = sortJSON(obj[i])\r",
													"            }\r",
													"        }\r",
													"        return obj;\r",
													"    } else {\r",
													"        for(const key of Object.keys(obj)) {\r",
													"            const value = obj[key];\r",
													"            if(Array.isArray(value)) {\r",
													"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
													"                    for(let i = 0; i < value.length; i++) {\r",
													"                        value[i] = sortJSON(value[i]);\r",
													"                    }\r",
													"                } \r",
													"            } else if(typeof(value) === 'object') {\r",
													"                obj[key] = sortJSON(value);\r",
													"            }\r",
													"        }\r",
													"    \r",
													"        return Object.fromEntries(\r",
													"            Object.entries(obj).sort(\r",
													"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
													"            )\r",
													"        );\r",
													"    }\r",
													"}"
												],
												"type": "text/javascript"
											}
										},
										{
											"listen": "test",
											"script": {
												"exec": [
													""
												],
												"type": "text/javascript"
											}
										}
									],
									"protocolProfileBehavior": {
										"disabledSystemHeaders": {}
									},
									"request": {
										"method": "POST",
										"header": [
											{
												"key": "sign",
												"value": "{{sign}}",
												"type": "text"
											}
										],
										"body": {
											"mode": "formdata",
											"formdata": [
												{
													"key": "appId",
													"value": "{{appId}}",
													"type": "text"
												},
												{
													"key": "timestamp",
													"value": "{{timestamp}}",
													"type": "text"
												},
												{
													"key": "bizData",
													"value": "{{bizData}}",
													"type": "text"
												}
											]
										},
										"url": "{{url}}/user/identifyH5"
									},
									"response": []
								},
								{
									"name": "个人用户认证API版",
									"event": [
										{
											"listen": "prerequest",
											"script": {
												"exec": [
													"// 组装请求参数\r",
													"function getBizData() {\r",
													"    const param = {};\r",
													"    param.name = ''; // 姓名\r",
													"    param.idCard = ''; // 身份证\r",
													"    param.mobile = ''; // 手机号码\r",
													"    param.bankCard = ''; // 银行卡号\r",
													"    param.identifyType = ''; // 实名认证类型\r",
													"    return param;\r",
													"}\r",
													"// 获取除文件外其他参数\r",
													"const bizData = getBizData();\r",
													"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
													"// 将请求数据bizData按键排序并转为字符串\r",
													"const jsonString = JSON.stringify(sortJSON(bizData));\r",
													"console.log(\"排序后参数：\" + jsonString);\r",
													"pm.variables.set('bizData', jsonString);\r",
													"// 获取时间戳并添加到表单中\r",
													"const timestamp = Date.now() + 1000 * 60 * 10;\r",
													"console.log('时间戳：' + timestamp);\r",
													"pm.variables.set('timestamp', timestamp);\r",
													"// 生成签名并添加到请求头中\r",
													"const sign = signature(jsonString, timestamp);\r",
													"console.log(\"签名：\" + sign);\r",
													"pm.variables.set('sign', sign);\r",
													"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
													"\r",
													"// 签名\r",
													"function signature(jsonString, timestamp) {\r",
													"    const signString = jsonString + \r",
													"        CryptoJS.MD5(jsonString) + \r",
													"        pm.variables.get('appId') + \r",
													"        timestamp;\r",
													"    eval(pm.variables.get('forgeJS'))\r",
													"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
													"    const md = forge.md.sha1.create();\r",
													"    md.update(signString, 'utf8');\r",
													"    return forge.util.encode64(privKey.sign(md));\r",
													"}\r",
													"\r",
													"// json排序\r",
													"function sortJSON(obj) {\r",
													"    if(Array.isArray(obj)) {\r",
													"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
													"            for(let i = 0; i < obj.length; i++) {\r",
													"                obj[i] = sortJSON(obj[i])\r",
													"            }\r",
													"        }\r",
													"        return obj;\r",
													"    } else {\r",
													"        for(const key of Object.keys(obj)) {\r",
													"            const value = obj[key];\r",
													"            if(Array.isArray(value)) {\r",
													"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
													"                    for(let i = 0; i < value.length; i++) {\r",
													"                        value[i] = sortJSON(value[i]);\r",
													"                    }\r",
													"                } \r",
													"            } else if(typeof(value) === 'object') {\r",
													"                obj[key] = sortJSON(value);\r",
													"            }\r",
													"        }\r",
													"    \r",
													"        return Object.fromEntries(\r",
													"            Object.entries(obj).sort(\r",
													"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
													"            )\r",
													"        );\r",
													"    }\r",
													"}"
												],
												"type": "text/javascript"
											}
										},
										{
											"listen": "test",
											"script": {
												"exec": [
													""
												],
												"type": "text/javascript"
											}
										}
									],
									"protocolProfileBehavior": {
										"disabledSystemHeaders": {}
									},
									"request": {
										"method": "POST",
										"header": [
											{
												"key": "sign",
												"value": "{{sign}}",
												"type": "text"
											}
										],
										"body": {
											"mode": "formdata",
											"formdata": [
												{
													"key": "appId",
													"value": "{{appId}}",
													"type": "text"
												},
												{
													"key": "timestamp",
													"value": "{{timestamp}}",
													"type": "text"
												},
												{
													"key": "bizData",
													"value": "{{bizData}}",
													"type": "text"
												}
											]
										},
										"url": "{{url}}/user/identify"
									},
									"response": []
								}
							]
						},
						{
							"name": "企业用户认证",
							"item": [
								{
									"name": "企业实名认证API版",
									"event": [
										{
											"listen": "prerequest",
											"script": {
												"exec": [
													"// 组装请求参数\r",
													"function getBizData() {\r",
													"    const param = {};\r",
													"    param.companyName = ''; // 企业名称\r",
													"    param.creditCode = ''; // 社会统一信用代码\r",
													"    param.name = ''; // 法人姓名\r",
													"    param.idCard = ''; // 法人身份证\r",
													"    param.notifyUrl = ''; // 实名认证回调通知URL\r",
													"    return param;\r",
													"}\r",
													"// 获取除文件外其他参数\r",
													"const bizData = getBizData();\r",
													"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
													"// 将请求数据bizData按键排序并转为字符串\r",
													"const jsonString = JSON.stringify(sortJSON(bizData));\r",
													"console.log(\"排序后参数：\" + jsonString);\r",
													"pm.variables.set('bizData', jsonString);\r",
													"// 获取时间戳并添加到表单中\r",
													"const timestamp = Date.now() + 1000 * 60 * 10;\r",
													"console.log('时间戳：' + timestamp);\r",
													"pm.variables.set('timestamp', timestamp);\r",
													"// 生成签名并添加到请求头中\r",
													"const sign = signature(jsonString, timestamp);\r",
													"console.log(\"签名：\" + sign);\r",
													"pm.variables.set('sign', sign);\r",
													"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
													"\r",
													"// 签名\r",
													"function signature(jsonString, timestamp) {\r",
													"    const signString = jsonString + \r",
													"        CryptoJS.MD5(jsonString) + \r",
													"        pm.variables.get('appId') + \r",
													"        timestamp;\r",
													"    eval(pm.variables.get('forgeJS'))\r",
													"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
													"    const md = forge.md.sha1.create();\r",
													"    md.update(signString, 'utf8');\r",
													"    return forge.util.encode64(privKey.sign(md));\r",
													"}\r",
													"\r",
													"// json排序\r",
													"function sortJSON(obj) {\r",
													"    if(Array.isArray(obj)) {\r",
													"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
													"            for(let i = 0; i < obj.length; i++) {\r",
													"                obj[i] = sortJSON(obj[i])\r",
													"            }\r",
													"        }\r",
													"        return obj;\r",
													"    } else {\r",
													"        for(const key of Object.keys(obj)) {\r",
													"            const value = obj[key];\r",
													"            if(Array.isArray(value)) {\r",
													"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
													"                    for(let i = 0; i < value.length; i++) {\r",
													"                        value[i] = sortJSON(value[i]);\r",
													"                    }\r",
													"                } \r",
													"            } else if(typeof(value) === 'object') {\r",
													"                obj[key] = sortJSON(value);\r",
													"            }\r",
													"        }\r",
													"    \r",
													"        return Object.fromEntries(\r",
													"            Object.entries(obj).sort(\r",
													"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
													"            )\r",
													"        );\r",
													"    }\r",
													"}"
												],
												"type": "text/javascript"
											}
										},
										{
											"listen": "test",
											"script": {
												"exec": [
													""
												],
												"type": "text/javascript"
											}
										}
									],
									"protocolProfileBehavior": {
										"disabledSystemHeaders": {}
									},
									"request": {
										"method": "POST",
										"header": [
											{
												"key": "sign",
												"value": "{{sign}}",
												"type": "text"
											}
										],
										"body": {
											"mode": "formdata",
											"formdata": [
												{
													"key": "appId",
													"value": "{{appId}}",
													"type": "text"
												},
												{
													"key": "timestamp",
													"value": "{{timestamp}}",
													"type": "text"
												},
												{
													"key": "bizData",
													"value": "{{bizData}}",
													"type": "text"
												}
											]
										},
										"url": "{{url}}/user/enterpriseIdentify"
									},
									"response": []
								},
								{
									"name": "法人授权签署认证",
									"event": [
										{
											"listen": "prerequest",
											"script": {
												"exec": [
													"// 组装请求参数\r",
													"function getBizData() {\r",
													"    const param = {};\r",
													"    param.account = '1'; // 用户唯一标识\r",
													"    param.companyName = ''; // 企业名称\r",
													"    param.creditCode = ''; // 社会统一信用代码\r",
													"    param.name = ''; // 法人姓名\r",
													"    param.idCard = ''; // 法人身份证\r",
													"    param.createEnt = false; // 是否添加企业账号\r",
													"    param.identifyMobile = ''; // 法人手机号\r",
													"    param.notifyUrl = ''; // 回调通知地址\r",
													"    param.redirectUrl = ''; // 同步跳转地址\r",
													"    return param;\r",
													"}\r",
													"// 获取除文件外其他参数\r",
													"const bizData = getBizData();\r",
													"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
													"// 将请求数据bizData按键排序并转为字符串\r",
													"const jsonString = JSON.stringify(sortJSON(bizData));\r",
													"console.log(\"排序后参数：\" + jsonString);\r",
													"pm.variables.set('bizData', jsonString);\r",
													"// 获取时间戳并添加到表单中\r",
													"const timestamp = Date.now() + 1000 * 60 * 10;\r",
													"console.log('时间戳：' + timestamp);\r",
													"pm.variables.set('timestamp', timestamp);\r",
													"// 生成签名并添加到请求头中\r",
													"const sign = signature(jsonString, timestamp);\r",
													"console.log(\"签名：\" + sign);\r",
													"pm.variables.set('sign', sign);\r",
													"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
													"\r",
													"// 签名\r",
													"function signature(jsonString, timestamp) {\r",
													"    const signString = jsonString + \r",
													"        CryptoJS.MD5(jsonString) + \r",
													"        pm.variables.get('appId') + \r",
													"        timestamp;\r",
													"    eval(pm.variables.get('forgeJS'))\r",
													"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
													"    const md = forge.md.sha1.create();\r",
													"    md.update(signString, 'utf8');\r",
													"    return forge.util.encode64(privKey.sign(md));\r",
													"}\r",
													"\r",
													"// json排序\r",
													"function sortJSON(obj) {\r",
													"    if(Array.isArray(obj)) {\r",
													"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
													"            for(let i = 0; i < obj.length; i++) {\r",
													"                obj[i] = sortJSON(obj[i])\r",
													"            }\r",
													"        }\r",
													"        return obj;\r",
													"    } else {\r",
													"        for(const key of Object.keys(obj)) {\r",
													"            const value = obj[key];\r",
													"            if(Array.isArray(value)) {\r",
													"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
													"                    for(let i = 0; i < value.length; i++) {\r",
													"                        value[i] = sortJSON(value[i]);\r",
													"                    }\r",
													"                } \r",
													"            } else if(typeof(value) === 'object') {\r",
													"                obj[key] = sortJSON(value);\r",
													"            }\r",
													"        }\r",
													"    \r",
													"        return Object.fromEntries(\r",
													"            Object.entries(obj).sort(\r",
													"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
													"            )\r",
													"        );\r",
													"    }\r",
													"}"
												],
												"type": "text/javascript"
											}
										},
										{
											"listen": "test",
											"script": {
												"exec": [
													""
												],
												"type": "text/javascript"
											}
										}
									],
									"protocolProfileBehavior": {
										"disabledSystemHeaders": {}
									},
									"request": {
										"method": "POST",
										"header": [
											{
												"key": "sign",
												"value": "{{sign}}",
												"type": "text"
											}
										],
										"body": {
											"mode": "formdata",
											"formdata": [
												{
													"key": "appId",
													"value": "{{appId}}",
													"type": "text"
												},
												{
													"key": "timestamp",
													"value": "{{timestamp}}",
													"type": "text"
												},
												{
													"key": "bizData",
													"value": "{{bizData}}",
													"type": "text"
												}
											]
										},
										"url": "{{url}}/user/legalAuthorization"
									},
									"response": []
								}
							]
						},
						{
							"name": "人脸核身认证",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.name = ''; // 姓名\r",
											"    param.idCard = ''; // 身份证号\r",
											"    param.notifyUrl = ''; // 人脸结果回调通知\r",
											"    param.faceAuthMode = ''; // 认证渠道\r",
											"    param.bizId = ''; // 接入商自定义业务ID\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/user/face"
							},
							"response": []
						},
						{
							"name": "查询人脸核身结果",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.bizId = ''; // 接入商自定义业务ID\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/user/faceResult"
							},
							"response": []
						},
						{
							"name": "发送短信验证码",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.mobile = ''; // 手机号\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/user/sendVerifyCode"
							},
							"response": []
						}
					]
				},
				{
					"name": "重新发送认证验证码",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.serialNo = ''; // 认证流水号\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"console.log(\"排序后参数：\" + jsonString);\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/auth/captcha/resend"
					},
					"response": []
				},
				{
					"name": "认证验证码校验",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.serialNo = ''; // 认证流水号\r",
									"    param.captcha = ''; // 短信验证码\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"console.log(\"排序后参数：\" + jsonString);\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/auth/captcha/verify"
					},
					"response": []
				},
				{
					"name": "实名认证信息查询",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.serialNo = ''; // 认证流水号\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"console.log(\"排序后参数：\" + jsonString);\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/auth/getAuthRecordInfo"
					},
					"response": []
				}
			]
		},
		{
			"name": "信息比对接口",
			"item": [
				{
					"name": "个人用户信息比对",
					"item": [
						{
							"name": "个人身份证二要素比对",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.realName = ''; // 姓名\r",
											"    param.idCardNo = ''; // 身份证\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/verify/person/idcard2"
							},
							"response": []
						},
						{
							"name": "个人银行卡三要素比对",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.realName = ''; // 姓名\r",
											"    param.idCardNo = ''; // 身份证\r",
											"    param.bankCard = ''; // 银行卡号\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/verify/person/bank3"
							},
							"response": []
						},
						{
							"name": "个人运营商三要素比对",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.realName = ''; // 姓名\r",
											"    param.idCardNo = ''; // 身份证\r",
											"    param.mobile = ''; // 手机号\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/verify/person/mobile3"
							},
							"response": []
						},
						{
							"name": "个人银行卡四要素比对",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.realName = ''; // 姓名\r",
											"    param.idCardNo = ''; // 身份证\r",
											"    param.mobile = ''; // 手机号\r",
											"    param.bankCard = ''; // 银行卡号\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/verify/person/bank4"
							},
							"response": []
						}
					]
				},
				{
					"name": "企业用户信息比对",
					"item": [
						{
							"name": "企业工商信息比对",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.realName = ''; // 法人姓名\r",
											"    param.companyName = ''; // 企业名称\r",
											"    param.creditCode = ''; // 社会统一信息用代码\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/verify/company/bizInfo"
							},
							"response": []
						},
						{
							"name": "企业四要素比对",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.realName = ''; // 法人姓名\r",
											"    param.idCardNo = ''; // 法人身份证号\r",
											"    param.companyName = ''; // 企业名称\r",
											"    param.creditCode = ''; // 社会统一信息用代码\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"console.log(\"排序后参数：\" + jsonString);\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/verify/company/ent4"
							},
							"response": []
						}
					]
				},
				{
					"name": "OCR身份证识别",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.side = 'front'; // 身份证正反\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"console.log(\"文件信息：\" + JSON.stringify(getFilepath()));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// 获取表单中的文件信息\r",
									"function getFilepath() {\r",
									"    let files = [];\r",
									"    for(const item of pm.request.body.formdata.members) {\r",
									"        if(item.type === 'file') {\r",
									"            files.push(item.src);\r",
									"        }\r",
									"    }\r",
									"    if(files.length === 1) {\r",
									"        return files[0];\r",
									"    } else {\r",
									"        return files;\r",
									"    }\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								},
								{
									"key": "image",
									"type": "file",
									"src": "/D:/TestFile/2.png"
								}
							]
						},
						"url": "{{url}}/user/ocrIdentify"
					},
					"response": []
				},
				{
					"name": "OCR银行卡识别",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.image = ''; // 银行卡图片base64字符串\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/ocr/bank"
					},
					"response": []
				}
			]
		},
		{
			"name": "用户接口",
			"item": [
				{
					"name": "修改用户信息",
					"item": [
						{
							"name": "发送验证码（修改用户绑定手机号）",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.account = ''; // 用户唯一识别码\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/user/sendCode"
							},
							"response": []
						},
						{
							"name": "修改手机号（验证码方式）",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.account = ''; // 用户唯一识别码\r",
											"    param.mobile = ''; // 用户新手机号\r",
											"    param.code = ''; // 验证码\r",
											"    param.ctoken = ''; // 调用发送短信接口返回的codeToken\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/user/modifyMobileByCode"
							},
							"response": []
						},
						{
							"name": "修改法人手机号（运营商三要素校验）",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.account = ''; // 用户唯一识别码\r",
											"    param.name = ''; // 用户姓名\r",
											"    param.idCard = ''; // 法人身份证号\r",
											"    param.mobile = ''; // 新手机号\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/user/modifyMobile"
							},
							"response": []
						},
						{
							"name": "修改手机号（无校验）",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.account = ''; // 用户唯一识别码\r",
											"    param.mobile = ''; // 新手机号\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/user/updateMobile"
							},
							"response": []
						},
						{
							"name": "修改用户签署密码",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.account = ''; // 用户唯一识别码\r",
											"    param.oldSignPwd = ''; // 旧签署密码\r",
											"    param.signPwd = ''; // 新签署密码\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/user/signPasswd"
							},
							"response": []
						},
						{
							"name": "初始化签约密码",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.account = ''; // 用户唯一识别码\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/user/startModSignPasswd"
							},
							"response": []
						},
						{
							"name": "重置签署密码",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.account = ''; // 用户唯一识别码\r",
											"    param.signPwd = ''; // 新签署密码\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/user/resetSignPwd"
							},
							"response": []
						},
						{
							"name": "修改企业信息",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.account = ''; // 用户唯一识别码\r",
											"    param.companyName = ''; // 企业名称\r",
											"    param.name = ''; // 法人名称\r",
											"    param.idCard = ''; // 法人身份证\r",
											"    param.idCardType = ''; // 法人证件类型\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/user/modifyCompanyInfo"
							},
							"response": []
						}
					]
				},
				{
					"name": "历史接口",
					"item": [
						{
							"name": "添加个人用户",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.account = ''; // 用户唯一识别码\r",
											"    param.name = ''; // 姓名\r",
											"    param.idCard = ''; // 个人身份证、台胞证、港澳通行证等证件号\r",
											"    param.idCardType = ''; // 证件类型\r",
											"    param.mobile = ''; // 手机号\r",
											"    param.signPwd = ''; // 签约密码明文\r",
											"    param.isSignPwdNotice = 0; // 是否将签署密码以短信形式通知用户\r",
											"    param.identifyType = ''; // 用户实名认证模式为强制认证时的认证方法\r",
											"    param.identifyMobile = ''; // 用户实名认证模式为强制认证时，用户实名认证的手机号\r",
											"    param.bankCard = ''; // 用户实名认证模式为强制认证时，用户实名认证的银行卡号\r",
											"    param.isNotice = 0; // 用户发起合同或需要签署时是否进行短信通知\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/user/addPersonalUser"
							},
							"response": []
						},
						{
							"name": "添加企业用户",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.account = ''; // 用户唯一识别码\r",
											"    param.companyName = ''; // 企业名称\r",
											"    param.creditCode = ''; // 社会统一信用代码\r",
											"    param.name = ''; // 法人姓名\r",
											"    param.idCard = ''; // 法人身份证、台胞证、港澳通行证等证件号\r",
											"    param.idCardType = ''; // 证件类型\r",
											"    param.identifyMobile = ''; // 法人手机号\r",
											"    param.isSignPwdNotice = 0; // 是否将签署密码以短信形式通知用户\r",
											"    param.isNotice = 0; // 用户发起合同或需要签署时是否进行短信通知\r",
											"    param.signPwd = ''; // 签约密码明文\r",
											"    param.mobile = ''; // 手机号\r",
											"    param.contactName = ''; // 联系人姓名\r",
											"    param.contactIdCard = ''; // 联系人身份证\r",
											"    param.identifyType = ''; // 用户实名认证模式为强制认证时法人实名认证类型\r",
											"    param.bankCard = ''; // 银行卡四要素认证时使用的银行卡号\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/user/addEnterpriseUser"
							},
							"response": []
						},
						{
							"name": "添加陌生用户",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.account = ''; // 用户唯一识别码\r",
											"    param.userType = ''; // 用户类型\r",
											"    param.name = ''; // 个人/企业法人用户姓名\r",
											"    param.companyName = ''; // 企业名称\r",
											"    param.mobile = ''; // 接收通知手机号\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/user/addStranger"
							},
							"response": []
						}
					]
				},
				{
					"name": "添加个人用户（V2）",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.account = ''; // 用户唯一识别码\r",
									"    param.serialNo = ''; // 实名认证流水号\r",
									"    param.name = ''; // 用户姓名\r",
									"    param.idCard = ''; // 个人身份证、台胞证、港澳通行证等证件号\r",
									"    param.idCardType = ''; // 证件类型\r",
									"    param.mobile = ''; // 手机号码\r",
									"    param.signPwd = ''; // 签约密码明文\r",
									"    param.isSignPwdNotice = 0; // 是否将签署密码以短信形式通知用户\r",
									"    param.isNotice = 0; // 用户发起合同或需要签署时是否进行短信通知\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/v2/user/addPersonalUser"
					},
					"response": []
				},
				{
					"name": "添加企业用户（V2）",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.account = ''; // 用户唯一识别码\r",
									"    param.serialNo = ''; // 实名认证流水号\r",
									"    param.companyName = ''; // 企业名称\r",
									"    param.creditCode = ''; // 企业社会统一信用代码\r",
									"    param.name = ''; // 用户姓名\r",
									"    param.idCard = ''; // 个人身份证、台胞证、港澳通行证等证件号\r",
									"    param.idCardType = ''; // 证件类型\r",
									"    param.mobile = ''; // 手机号码\r",
									"    param.contactName = ''; // 联系人姓名\r",
									"    param.contactIdCard = ''; // 联系人身份证号\r",
									"    param.signPwd = ''; // 签约密码明文\r",
									"    param.isSignPwdNotice = 0; // 是否将签署密码以短信形式通知用户\r",
									"    param.isNotice = 0; // 用户发起合同或需要签署时是否进行短信通知\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/v2/user/addEnterpriseUser"
					},
					"response": []
				},
				{
					"name": "添加陌生用户（V2）",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.account = ''; // 用户唯一识别码\r",
									"    param.userType = ''; // 用户类型\r",
									"    param.name = ''; // 个人用户姓名/企业法人姓名\r",
									"    param.companyName = ''; // 企业名称\r",
									"    param.mobile = ''; // 手机号码\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/v2/user/addStranger"
					},
					"response": []
				},
				{
					"name": "同步陌生个人信息",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.account = ''; // 用户唯一识别码\r",
									"    param.name = ''; // 用户姓名\r",
									"    param.mobile = ''; // 用户手机号\r",
									"    param.idCard = ''; // 用户证件号\r",
									"    param.idCardType = ''; // 证件类型\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/user/authPersonalStranger"
					},
					"response": []
				},
				{
					"name": "同步陌生企业信息",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.account = ''; // 用户唯一识别码\r",
									"    param.name = ''; // 用户姓名\r",
									"    param.mobile = ''; // 用户手机号\r",
									"    param.idCard = ''; // 用户证件号\r",
									"    param.companyName = ''; // 企业名称\r",
									"    param.creditCode = ''; // 社会统一信用代码\r",
									"    param.contactName = ''; // 企业联系人姓名\r",
									"    param.contactPhone = ''; // 企业联系人手机号\r",
									"    param.contactIdCard = ''; // 企业联系人身份证号\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/user/authEnterpriseStranger"
					},
					"response": []
				},
				{
					"name": "查询用户信息",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.account = ''; // 用户唯一识别码\r",
									"    param.idCard = ''; // 用户证件号\r",
									"    param.creditCode = ''; // 社会统一信用代码\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/user/getUser"
					},
					"response": []
				}
			]
		},
		{
			"name": "印章接口",
			"item": [
				{
					"name": "创建印章",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.account = ''; // 用户唯一识别码\r",
									"    param.isDefault = ''; // 是否设置为默认印章\r",
									"    param.sealName = ''; // 印章名称\r",
									"    param.sealNo = ''; // 印章唯一编号\r",
									"    param.scaling = ''; // 图片缩放百分比\r",
									"    param.color = ''; // 印章颜色\r",
									"    param.hasBorder = 0; // 是否带边框\r",
									"    param.shape = ''; // 边框样式\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"console.log(\"文件信息：\" + JSON.stringify(getFilepath()));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// 获取表单中的文件信息\r",
									"function getFilepath() {\r",
									"    let files = [];\r",
									"    for(const item of pm.request.body.formdata.members) {\r",
									"        if(item.type === 'file') {\r",
									"            files.push(item.src);\r",
									"        }\r",
									"    }\r",
									"    if(files.length === 1) {\r",
									"        return files[0];\r",
									"    } else {\r",
									"        return files;\r",
									"    }\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								},
								{
									"key": "image",
									"type": "file",
									"src": "/D:/TestFile/1.png"
								}
							]
						},
						"url": "{{url}}/user/createSeal"
					},
					"response": []
				},
				{
					"name": "修改印章",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.account = ''; // 用户唯一识别码\r",
									"    param.isDefault = ''; // 是否设置为默认印章\r",
									"    param.sealName = ''; // 印章名称\r",
									"    param.sealNo = ''; // 印章唯一编号\r",
									"    param.scaling = ''; // 图片缩放百分比\r",
									"    param.color = ''; // 印章颜色\r",
									"    param.hasBorder = 0; // 是否带边框\r",
									"    param.shape = ''; // 边框样式\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"console.log(\"文件信息：\" + JSON.stringify(getFilepath()));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// 获取表单中的文件信息\r",
									"function getFilepath() {\r",
									"    let files = [];\r",
									"    for(const item of pm.request.body.formdata.members) {\r",
									"        if(item.type === 'file') {\r",
									"            files.push(item.src);\r",
									"        }\r",
									"    }\r",
									"    if(files.length === 1) {\r",
									"        return files[0];\r",
									"    } else {\r",
									"        return files;\r",
									"    }\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								},
								{
									"key": "image",
									"type": "file",
									"src": "/D:/TestFile/1.png"
								}
							]
						},
						"url": "{{url}}/user/modifySeal"
					},
					"response": []
				},
				{
					"name": "删除印章",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.account = ''; // 用户唯一识别码\r",
									"    param.sealNo = ''; // 印章编号\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/user/removeSeal"
					},
					"response": []
				},
				{
					"name": "设置默认章",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.account = ''; // 用户唯一识别码\r",
									"    param.sealNo = ''; // 印章编号\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/user/setDefaultSeal"
					},
					"response": []
				},
				{
					"name": "查询印章",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.account = ''; // 用户唯一识别码\r",
									"    param.sealNo = ''; // 印章编号\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/user/getUserSeals"
					},
					"response": []
				}
			]
		},
		{
			"name": "签署接口",
			"item": [
				{
					"name": "上传待签署文件",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.contractNo = 'yr20230824002'; // 合同唯一编号\r",
									"    param.contractName = '元让测试合同'; // 合同名称\r",
									"    param.validityTime = '30'; // 合同有效天数\r",
									"    param.validityDate = ''; // 合同有效截止日期\r",
									"    param.signOrder = 1; // 签约方式\r",
									"    \r",
									"    const templates = [];\r",
									"    // 添加一项模板 START 多项模板请自行添加\r",
									"    const template1 = {};\r",
									"    template1.templateNo = 'yr20221215001'; // 模板编号\r",
									"    template1.contractNo = ''; // 合同编号\r",
									"    template1.fillData = {}; // 单行文本、多行文本、日期、身份证类型参数填充。请自行按照接口文档进行json组装\r",
									"    template1.componentData = []; // 单选、复选、勾选、图片类型参数填充。请自行按照接口文档进行json组装\r",
									"    template1.tableDatas = []; // 表格类型数据填充。请自行按照接口文档进行json组装\r",
									"    templates.push(template1);\r",
									"    // 添加一项模板 END\r",
									"    param.templates = templates; // 合同模板列表\r",
									"    param.readSeconds = ''; // 强制阅读时间\r",
									"    param.readType = ''; // 强制阅读设置\r",
									"    param.needAgree = 0; // 同意协议开关\r",
									"    param.autoExpand = 0; // 是否自动展开文件列表\r",
									"    param.notifyUrl = ''; // 签署完成异步回调通知\r",
									"    param.userNotifyUrl = ''; // 单个用户签署完成异步回调通知\r",
									"    param.redirectUrl = ''; // 签署完成后同步跳转地址\r",
									"    param.refuseOn = 0; // 合同签署页退回按钮开关\r",
									"    param.autoContinue = 0; // 顺序签时当前用户签署完成自动跳转到下一签署人页面开关\r",
									"    param.viewFlg = ''; // 合同签署完是否允许可以通过链接查看合同内容\r",
									"    param.redirectReturnUrl = ''; // 合同发起页面返回按钮跳转url 若不为空，可以跳转业务方自己的前端页面\r",
									"    param.redirectCompletedUrl = ''; // 合同发起页面完成后跳转url 若不为空，可以跳转业务方自己的前端过渡页面，实现业务方自己的逻辑\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"console.log(\"文件信息：\" + JSON.stringify(getFilepath()));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// 获取表单中的文件信息\r",
									"function getFilepath() {\r",
									"    let files = [];\r",
									"    for(const item of pm.request.body.formdata.members) {\r",
									"        if(item.type === 'file') {\r",
									"            files.push(item.src);\r",
									"        }\r",
									"    }\r",
									"    if(files.length === 1) {\r",
									"        return files[0];\r",
									"    } else {\r",
									"        return files;\r",
									"    }\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								},
								{
									"key": "contractFiles",
									"type": "file",
									"src": [
										"/D:/TestFile/loan_card_template.pdf",
										"/D:/TestFile/单页.pdf"
									]
								}
							]
						},
						"url": "{{url}}/contract/createContract"
					},
					"response": []
				},
				{
					"name": "添加签署方",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = [];\r",
									"    // 添加一个签署人 START 若需添加多个签署人，则自行拼装添加\r",
									"    const signer1 = {}\r",
									"    signer1.contractNo = ''; // 合同唯一编号\r",
									"    signer1.account = ''; // 用户唯一标识\r",
									"    signer1.signType = 3; // 签约方式\r",
									"    signer1.sealNo = ''; // 印章编号\r",
									"    signer1.noticeMobile = ''; // 签约短信通知手机\r",
									"    signer1.noticeEmail = ''; // 签约短信通知邮箱号\r",
									"    signer1.signOrder = 1; // 签约顺序，从1开始，无序签约均为1\r",
									"    signer1.isNotice = 0; // 是否接受合同签署链接的短信通知\r",
									"    signer1.validateType = 1; // 签约方式\r",
									"    signer1.validateTypeList = ''; // 组合签署方式\r",
									"    signer1.faceAuthMode = ''; // 人脸签约人脸识别渠道\r",
									"    signer1.autoSwitch = 1; // 自动切换签约方式\r",
									"    signer1.isNoticeComplete = 0; // 合同签署完成后是否通知用户\r",
									"    signer1.waterMark = 0; // 是否在距底部10px中央位置添加日期水印\r",
									"    signer1.autoSms = 1; // 是否自动触发验证码短信\r",
									"    signer1.customSignFlag = 0; // 签章位置策略\r",
									"    signer1.signStrategyList = []; // 签章策略，请自行按照接口文档进行json组装\r",
									"    signer1.signStrikeList = []; // 骑缝章策略，请自行按照接口文档进行json组装\r",
									"    signer1.receiverFillStrategyList = []; // 接收方模板填充策略，请自行按照接口文档进行json组装\r",
									"    param.authConfig = {}; // 陌生人签署认证设置\r",
									"    param.authConfig.authTypeList = []; // 选择显示给个人/办理人的页面认证方式\r",
									"    param.authConfig.companyAuthTypeList = []; // 选择显示给公司的页面认证方式\r",
									"    param.authConfig.showAuthPage = '1'; // 是否展示授权页\r",
									"    param.authConfig.faceAuthMode = ''; // 认证人脸识别方式\r",
									"\r",
									"    param.push(signer1);\r",
									"    // 添加一个签署人 END\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/contract/addSigner"
					},
					"response": []
				},
				{
					"name": "添加签署方(通过页面发起签署)",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = [];\r",
									"    // 添加一个签署人 START 若需添加多个签署人，则自行拼装添加\r",
									"    const signer1 = {}\r",
									"    signer1.contractNo = ''; // 合同唯一编号\r",
									"    signer1.account = ''; // 用户唯一标识\r",
									"    signer1.signType = 3; // 签约方式\r",
									"    signer1.sealNo = ''; // 印章编号\r",
									"    signer1.noticeMobile = ''; // 签约短信通知手机\r",
									"    signer1.noticeEmail = ''; // 签约短信通知邮箱号\r",
									"    signer1.signOrder = 1; // 签约顺序，从1开始，无序签约均为1\r",
									"    signer1.isNotice = 0; // 是否接受合同签署链接的短信通知\r",
									"    signer1.validateType = 1; // 签约方式\r",
									"    signer1.validateTypeList = ''; // 组合签署方式\r",
									"    signer1.faceAuthMode = ''; // 人脸签约人脸识别渠道\r",
									"    signer1.autoSwitch = 1; // 自动切换签约方式\r",
									"    signer1.isNoticeComplete = 0; // 合同签署完成后是否通知用户\r",
									"    signer1.waterMark = 0; // 是否在距底部10px中央位置添加日期水印\r",
									"    signer1.autoSms = 1; // 是否自动触发验证码短信\r",
									"    signer1.customSignFlag = 0; // 签章位置策略\r",
									"   \r",
									"    param.push(signer1);\r",
									"    // 添加一个签署人 END\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/contract/addEmbeddedSigner"
					},
					"response": []
				},
				{
					"name": "查询合同状态",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.contractNo = ''; // 合同唯一编号\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/contract/status"
					},
					"response": []
				},
				{
					"name": "查询合同信息",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.contractNo = ''; // 合同唯一编号\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/contract/getContract"
					},
					"response": []
				},
				{
					"name": "预览合同",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.contractNo = ''; // 合同唯一编号\r",
									"    param.account = ''; // 用户唯一识别码\r",
									"    param.isWrite = ''; // 是否手写章\r",
									"    param.signStrategyList = []; // 展示用签约策略，请自行按照接口文档进行json组装\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/contract/previewContract"
					},
					"response": []
				},
				{
					"name": "下载合同",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.contractNo = ''; // 合同唯一编号\r",
									"    param.force = 0; // 强制下载标识\r",
									"    param.downloadFileType = ''; // 下载文件类型\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/contract/downloadContract"
					},
					"response": []
				},
				{
					"name": "下载指定合同附件",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.contractNo = ''; // 合同唯一编号\r",
									"    param.attachNames = []; // 附件名称集合\r",
									"    param.downloadFileType = ''; // 下载文件类型\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/contract/downloadByAttachName"
					},
					"response": []
				},
				{
					"name": "合同作废",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.contractNo = ''; // 合同唯一编号\r",
									"    param.validityTime = ''; // 签约截止天数\r",
									"    param.notifyUrl = ''; // 签署完成后回调URL\r",
									"    param.redirectUrl = ''; // 签署完成同步跳转URL\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/contract/cancellation"
					},
					"response": []
				},
				{
					"name": "发起用户批量签署",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.account = ''; // 用户唯一识别码\r",
									"    param.mobile = ''; // 签约短信通知手机号\r",
									"    param.contractNos = []; // 指定需要用户批量签署的合同编号\r",
									"    param.forcedRead = ''; // 是否强制阅读合同\r",
									"    param.validateType = ''; // 签署方式指定\r",
									"    param.validateTypeList = []; // 组合签署方式指定\r",
									"    param.faceAuthMode = ''; // 人脸识别方式\r",
									"    param.redirectUrl = ''; // 签署完成同步跳转地址\r",
									"    param.authConfig = {}; // 陌生人签署认证设置\r",
									"    param.authConfig.authTypeList = []; // 选择显示给个人/办理人的页面认证方式\r",
									"    param.authConfig.companyAuthTypeList = []; // 选择显示给公司的页面认证方式\r",
									"    param.authConfig.showAuthPage = '1'; // 是否展示授权页\r",
									"    param.authConfig.faceAuthMode = ''; // 认证人脸识别方式\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/v2/contract/batchSignByAccount"
					},
					"response": []
				}
			]
		},
		{
			"name": "模板接口",
			"item": [
				{
					"name": "嵌入式模板制作",
					"item": [
						{
							"name": "模板文件上传接口",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.templateIdent = ''; // 模板编号\r",
											"    param.isCover = '0'; // 是否覆盖原文件\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"console.log(\"文件信息：\" + JSON.stringify(getFilepath()));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// 获取表单中的文件信息\r",
											"function getFilepath() {\r",
											"    let files = [];\r",
											"    for(const item of pm.request.body.formdata.members) {\r",
											"        if(item.type === 'file') {\r",
											"            files.push(item.src);\r",
											"        }\r",
											"    }\r",
											"    if(files.length === 1) {\r",
											"        return files[0];\r",
											"    } else {\r",
											"        return files;\r",
											"    }\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										},
										{
											"key": "file",
											"type": "file",
											"src": "/D:/TestFile/单页.pdf"
										}
									]
								},
								"url": "{{url}}/template/upload"
							},
							"response": []
						},
						{
							"name": "新增/编辑模板",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.templateIdent = ''; // 模板编号\r",
											"    param.redirectUrl = ''; // 完成后跳转地址\r",
											"    param.notifyUrl = ''; // 异步回调通知\r",
											"    param.hiddenBasic = '0'; // 是否隐藏基础控件\r",
											"    param.isControlDelete = '0'; // 是否删除模板中已存在的指定控件\r",
											"    param.controlData = [{\r",
											"        'dataType': '', // 控件类型\r",
											"        'dataKey': '', // 控件名称\r",
											"        'fillType': '', // 填充方设置\r",
											"        'needDrag': '1', // 控件是否必须拖到pdf中\r",
											"        'required': '1' // 控件是否必填项\r",
											"    }]; // 指定控件数据\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/template/open"
							},
							"response": []
						},
						{
							"name": "同步模板",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.templateIdent = ''; // 模板编号\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/template/sync"
							},
							"response": []
						},
						{
							"name": "撤销同步模板",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.templateIdent = ''; // 模板编号\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/template/sync/cancel"
							},
							"response": []
						}
					]
				},
				{
					"name": "下载模板",
					"item": [
						{
							"name": "下载未同步模板",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.templateNo = ''; // 模板编号\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/contract/downloadTemplate"
							},
							"response": []
						},
						{
							"name": "下载未同步模板 Copy",
							"event": [
								{
									"listen": "prerequest",
									"script": {
										"exec": [
											"// 组装请求参数\r",
											"function getBizData() {\r",
											"    const param = {};\r",
											"    param.templateIdent = ''; // 模板编号\r",
											"    param.fillData = []; // 填充数据。请自行按照接口文档进行json组装\r",
											"    return param;\r",
											"}\r",
											"// 获取除文件外其他参数\r",
											"const bizData = getBizData();\r",
											"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
											"// 将请求数据bizData按键排序并转为字符串\r",
											"const jsonString = JSON.stringify(sortJSON(bizData));\r",
											"pm.variables.set('bizData', jsonString);\r",
											"// 获取时间戳并添加到表单中\r",
											"const timestamp = Date.now() + 1000 * 60 * 10;\r",
											"console.log('时间戳：' + timestamp);\r",
											"pm.variables.set('timestamp', timestamp);\r",
											"// 生成签名并添加到请求头中\r",
											"const sign = signature(jsonString, timestamp);\r",
											"console.log(\"签名：\" + sign);\r",
											"pm.variables.set('sign', sign);\r",
											"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
											"\r",
											"// 签名\r",
											"function signature(jsonString, timestamp) {\r",
											"    const signString = jsonString + \r",
											"        CryptoJS.MD5(jsonString) + \r",
											"        pm.variables.get('appId') + \r",
											"        timestamp;\r",
											"    eval(pm.variables.get('forgeJS'))\r",
											"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
											"    const md = forge.md.sha1.create();\r",
											"    md.update(signString, 'utf8');\r",
											"    return forge.util.encode64(privKey.sign(md));\r",
											"}\r",
											"\r",
											"// json排序\r",
											"function sortJSON(obj) {\r",
											"    if(Array.isArray(obj)) {\r",
											"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
											"            for(let i = 0; i < obj.length; i++) {\r",
											"                obj[i] = sortJSON(obj[i])\r",
											"            }\r",
											"        }\r",
											"        return obj;\r",
											"    } else {\r",
											"        for(const key of Object.keys(obj)) {\r",
											"            const value = obj[key];\r",
											"            if(Array.isArray(value)) {\r",
											"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
											"                    for(let i = 0; i < value.length; i++) {\r",
											"                        value[i] = sortJSON(value[i]);\r",
											"                    }\r",
											"                } \r",
											"            } else if(typeof(value) === 'object') {\r",
											"                obj[key] = sortJSON(value);\r",
											"            }\r",
											"        }\r",
											"    \r",
											"        return Object.fromEntries(\r",
											"            Object.entries(obj).sort(\r",
											"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
											"            )\r",
											"        );\r",
											"    }\r",
											"}"
										],
										"type": "text/javascript"
									}
								},
								{
									"listen": "test",
									"script": {
										"exec": [
											""
										],
										"type": "text/javascript"
									}
								}
							],
							"protocolProfileBehavior": {
								"disabledSystemHeaders": {}
							},
							"request": {
								"method": "POST",
								"header": [
									{
										"key": "sign",
										"value": "{{sign}}",
										"type": "text"
									}
								],
								"body": {
									"mode": "formdata",
									"formdata": [
										{
											"key": "appId",
											"value": "{{appId}}",
											"type": "text"
										},
										{
											"key": "timestamp",
											"value": "{{timestamp}}",
											"type": "text"
										},
										{
											"key": "bizData",
											"value": "{{bizData}}",
											"type": "text"
										}
									]
								},
								"url": "{{url}}/template/download"
							},
							"response": []
						}
					]
				},
				{
					"name": "本地模板上传",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.templateName = ''; // 模板名称\r",
									"    param.templateNo = ''; // 模板编号\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"console.log(\"文件信息：\" + JSON.stringify(getFilepath()));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// 获取表单中的文件信息\r",
									"function getFilepath() {\r",
									"    let files = [];\r",
									"    for(const item of pm.request.body.formdata.members) {\r",
									"        if(item.type === 'file') {\r",
									"            files.push(item.src);\r",
									"        }\r",
									"    }\r",
									"    if(files.length === 1) {\r",
									"        return files[0];\r",
									"    } else {\r",
									"        return files;\r",
									"    }\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								},
								{
									"key": "templateFile",
									"type": "file",
									"src": "/D:/TestFile/单页.pdf"
								}
							]
						},
						"url": "{{url}}/contract/uploadTemplate"
					},
					"response": []
				},
				{
					"name": "删除模板",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.templateIdent = ''; // 模板编号\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/template/delete"
					},
					"response": []
				},
				{
					"name": "查询模板列表",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.templateIdent = ''; // 模板编号\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/template/data"
					},
					"response": []
				}
			]
		},
		{
			"name": "存证接口",
			"item": [
				{
					"name": "下载存证报告",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.contractNo = ''; // 合同唯一编号\r",
									"    param.preserveNo = ''; // 存证编号\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/contract/downloadWithCert"
					},
					"response": []
				},
				{
					"name": "下载区块链存证报告",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.contractNo = ''; // 合同唯一编号\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/contract/downloadChainReport"
					},
					"response": []
				}
			]
		},
		{
			"name": "PDF摘要签署接口",
			"item": [
				{
					"name": "获取证书",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.account = ''; // 用户唯一标识\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/user/cert"
					},
					"response": []
				},
				{
					"name": "计算签名",
					"event": [
						{
							"listen": "prerequest",
							"script": {
								"exec": [
									"// 组装请求参数\r",
									"function getBizData() {\r",
									"    const param = {};\r",
									"    param.account = ''; // 用户唯一标识\r",
									"    param.hash = ''; // 文件SHA256值\r",
									"    return param;\r",
									"}\r",
									"// 获取除文件外其他参数\r",
									"const bizData = getBizData();\r",
									"console.log(\"请求参数：\" + JSON.stringify(bizData));\r",
									"// 将请求数据bizData按键排序并转为字符串\r",
									"const jsonString = JSON.stringify(sortJSON(bizData));\r",
									"pm.variables.set('bizData', jsonString);\r",
									"// 获取时间戳并添加到表单中\r",
									"const timestamp = Date.now() + 1000 * 60 * 10;\r",
									"console.log('时间戳：' + timestamp);\r",
									"pm.variables.set('timestamp', timestamp);\r",
									"// 生成签名并添加到请求头中\r",
									"const sign = signature(jsonString, timestamp);\r",
									"console.log(\"签名：\" + sign);\r",
									"pm.variables.set('sign', sign);\r",
									"console.log(\"Postman前置脚本执行结束，开始发起请求\")\r",
									"\r",
									"// 签名\r",
									"function signature(jsonString, timestamp) {\r",
									"    const signString = jsonString + \r",
									"        CryptoJS.MD5(jsonString) + \r",
									"        pm.variables.get('appId') + \r",
									"        timestamp;\r",
									"    eval(pm.variables.get('forgeJS'))\r",
									"    const privKey = forge.pki.privateKeyFromPem(pm.variables.get('privateKey'));\r",
									"    const md = forge.md.sha1.create();\r",
									"    md.update(signString, 'utf8');\r",
									"    return forge.util.encode64(privKey.sign(md));\r",
									"}\r",
									"\r",
									"// json排序\r",
									"function sortJSON(obj) {\r",
									"    if(Array.isArray(obj)) {\r",
									"        if(obj.length !== 0 && typeof(obj[0] === 'object')) {\r",
									"            for(let i = 0; i < obj.length; i++) {\r",
									"                obj[i] = sortJSON(obj[i])\r",
									"            }\r",
									"        }\r",
									"        return obj;\r",
									"    } else {\r",
									"        for(const key of Object.keys(obj)) {\r",
									"            const value = obj[key];\r",
									"            if(Array.isArray(value)) {\r",
									"                if(value.length !== 0 && typeof(value[0]) === 'object') {\r",
									"                    for(let i = 0; i < value.length; i++) {\r",
									"                        value[i] = sortJSON(value[i]);\r",
									"                    }\r",
									"                } \r",
									"            } else if(typeof(value) === 'object') {\r",
									"                obj[key] = sortJSON(value);\r",
									"            }\r",
									"        }\r",
									"    \r",
									"        return Object.fromEntries(\r",
									"            Object.entries(obj).sort(\r",
									"                (o1, o2) => o1[0].localeCompare(o2[0])\r",
									"            )\r",
									"        );\r",
									"    }\r",
									"}"
								],
								"type": "text/javascript"
							}
						},
						{
							"listen": "test",
							"script": {
								"exec": [
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"protocolProfileBehavior": {
						"disabledSystemHeaders": {}
					},
					"request": {
						"method": "POST",
						"header": [
							{
								"key": "sign",
								"value": "{{sign}}",
								"type": "text"
							}
						],
						"body": {
							"mode": "formdata",
							"formdata": [
								{
									"key": "appId",
									"value": "{{appId}}",
									"type": "text"
								},
								{
									"key": "timestamp",
									"value": "{{timestamp}}",
									"type": "text"
								},
								{
									"key": "bizData",
									"value": "{{bizData}}",
									"type": "text"
								}
							]
						},
						"url": "{{url}}/user/makeSign"
					},
					"response": []
				}
			]
		}
	],
	"event": [
		{
			"listen": "prerequest",
			"script": {
				"type": "text/javascript",
				"exec": [
					"pm.sendRequest(pm.variables.get('forgeUrl'), function(err, res) {",
					"    if(err) {",
					"        console.log('forgeJS载入失败：' + err);",
					"        return;",
					"    } else {",
					"        console.log('forgeJS载入成功')",
					"        pm.variables.set('forgeJS', res.text());",
					"    }",
					"});"
				]
			}
		},
		{
			"listen": "test",
			"script": {
				"type": "text/javascript",
				"exec": [
					"console.log('响应状态码：' + responseCode.code)",
					"const response = JSON.parse(responseBody);",
					"if(response['code'] === 100000) {",
					"    console.log(\"请求成功：code = \" + response.code);",
					"} else {",
					"    console.log(\"请求失败：code = \" + response.code);",
					"}",
					"console.log('响应消息：' + response.msg);",
					"if(response.data) {",
					"    console.log('响应数据：' + JSON.stringify(response.data));",
					"}"
				]
			}
		}
	],
	"variable": [
		{
			"key": "appId",
			"value": "{appId}",
			"type": "string"
		},
		{
			"key": "url",
			"value": "{url}",
			"type": "string"
		},
		{
			"key": "privateKey",
			"value": "{privateKey}",
			"type": "string"
		},
		{
			"key": "timestamp",
			"value": "",
			"type": "string"
		},
		{
			"key": "sign",
			"value": "",
			"type": "string"
		},
		{
			"key": "bizData",
			"value": "",
			"type": "string"
		},
		{
			"key": "forgeUrl",
			"value": "{forgeUrl}",
			"type": "string"
		}
	]
}